defaultLogLevel: DEBUG
httpServer:
  host: localhost

application:
  database:
    dsn: postgres://sumweave_runtime:sumweave_runtime_local@127.0.0.1:55432/sumweave_test?sslmode=disable

agentRuntime:
  database:
    dsn: postgres://sumweave_runtime:sumweave_runtime_local@127.0.0.1:55432/sumweave_test?sslmode=disable

auth:
  jwtSigningKey: test-secret-key

finance:
  providers:
    enableBanking:
      baseURL: "https://enable-banking.test"
      appID: "app-test"
      privateKeyPath: "data/test-enable-banking-private-key.pem"
      aspspName: "Mock ASPSP"
      country: "PL"
      psuType: "personal"
      validDays: 90

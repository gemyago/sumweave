---
phase: 02-agent-profile-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/signal-foundry/internal/runtime.go
  - apps/signal-foundry/internal/runtime_test.go
  - docs/implementation/agent-profile-schema-boundary.md
autonomous: true
requirements: [AGNT-02, AGNT-03, PERS-01, PERS-02]
user_setup: []
must_haves:
  truths:
    - `apps/signal-foundry` wires the new runtime profile service into the mounted runtime API without introducing a second persistence selector or bypassing runtime contracts.
    - Database-backed app startup migrates profile tables alongside existing runtime tables when `agentRuntime.database.autoMigrate` is enabled.
    - The repo contains a durable schema and boundary reference that documents which profile fields are general Phase 2 data and which ACP or OpenCode connection details remain deferred to later phases.
  artifacts:
    - apps/signal-foundry/internal/runtime.go
    - apps/signal-foundry/internal/runtime_test.go
    - docs/implementation/agent-profile-schema-boundary.md
  key_links:
    - `apps/signal-foundry/internal/runtime.go` uses the existing `agentRuntime.storage.type` switch to choose file or database profile persistence instead of introducing a new config branch.
    - When database storage is active, `apps/signal-foundry/internal/runtime.go` calls profile-service migration before serving requests and still wires the service into `httpapi.NewHandler`.
    - `docs/implementation/agent-profile-schema-boundary.md` explicitly links the general `Agent` concept to deferred `Connection` details so Phase 3 can add OpenCode bindings without redefining the Phase 2 schema.
---

<objective>
Wire runtime profile services into `apps/signal-foundry`, handle database migration on startup, and publish the durable schema and boundary contract for later phases.

Purpose: The runtime pieces from Plans `02-01` and `02-02` only become usable when the app constructs them consistently and the repo documents the intended profile-vs-connection boundary.
Output: App-layer runtime wiring, startup migration orchestration, runtime wiring tests, and a durable implementation doc for the profile schema boundary.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-agent-profile-foundation/02-CONTEXT.md
@.planning/phases/02-agent-profile-foundation/02-RESEARCH.md
@docs/implementation/opencode-acp-capability-map.md
@docs/domain-terminology.md
@./AGENTS.md
@runtime/AGENTS.md
@apps/signal-foundry/AGENTS.md
@docs/golang-coding-guide.md
@apps/signal-foundry/internal/runtime.go
@apps/signal-foundry/internal/runtime_test.go
@apps/signal-foundry/internal/config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire agent profile services into app runtime startup and migration flow</name>
  <files>apps/signal-foundry/internal/runtime.go, apps/signal-foundry/internal/runtime_test.go</files>
  <read_first>AGENTS.md, apps/signal-foundry/AGENTS.md, runtime/AGENTS.md, docs/golang-coding-guide.md, apps/signal-foundry/internal/runtime.go, apps/signal-foundry/internal/runtime_test.go</read_first>
  <action>Extend `apps/signal-foundry/internal/runtime.go` to construct an `agent.AgentProfilesService` alongside the existing providers config service, using the same `agentRuntime.storage.type`, `dataDir`, database DSN, and table-prefix inputs already used for runtime persistence. Add a dedicated helper similar to `newProvidersConfigService` rather than inlining storage decisions. Pass the profile service into `httpapi.NewHandler` so the new runtime CRUD endpoints become available under the existing `/api/v1/runtime/` mount with no extra app routes. When database storage is enabled and `agentRuntime.database.autoMigrate` is true, run profile-service migration in the same startup path as `runner.AutoMigrate()` so profile tables are created before the handler serves requests. Expand `TestNewRuntime` to cover file-backed startup, database-backed startup, and database startup with auto-migrate disabled, ensuring the new profile service wiring does not break existing runtime construction.</action>
  <verify>cd apps/signal-foundry && go test ./internal -run TestNewRuntime</verify>
  <acceptance_criteria>
    - `apps/signal-foundry/internal/runtime.go` constructs profile persistence from the existing `agentRuntime` storage config instead of adding a second storage selector
    - `apps/signal-foundry/internal/runtime.go` passes the profile service into `httpapi.NewHandler`
    - database-backed startup calls profile-service migration when `agentRuntime.database.autoMigrate` is true and skips it when false
    - `apps/signal-foundry/internal/runtime_test.go` includes database and non-database runtime construction coverage after the profile-service wiring change
    - `cd apps/signal-foundry && go test ./internal -run TestNewRuntime` exits 0
  </acceptance_criteria>
  <done>The app runtime wires profile persistence and migration correctly for both supported storage modes.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the durable Phase 2 profile schema and boundary contract</name>
  <files>docs/implementation/agent-profile-schema-boundary.md</files>
  <read_first>.planning/phases/02-agent-profile-foundation/02-CONTEXT.md, .planning/phases/02-agent-profile-foundation/02-RESEARCH.md, docs/implementation/opencode-acp-capability-map.md, docs/domain-terminology.md, runtime/AGENTS.md, apps/signal-foundry/AGENTS.md</read_first>
  <action>Create `docs/implementation/agent-profile-schema-boundary.md` as the durable implementation reference for Phase 2. Document the canonical general profile shape shipped in Plans `02-01` and `02-02`: immutable identifier strategy, mutable fields, persisted storage locations for file and database modes, and the runtime HTTP endpoints added for CRUD. Include a `## General Profile Data` section and a `## Deferred Connection Or Backend Data` section, then add an explicit boundary table that separates the two. The deferred side must call out ACP and OpenCode-specific details that remain outside the general profile contract, including `cwd`, `mcpServers`, capability flags, remote session identifiers, slash-command inventories, and any backend-specific launch options. Tie the terminology back to the glossary distinction between `Agent` and `Connection` so Phase 3 can add OpenCode bindings without redefining the saved profile schema.</action>
  <verify>test -f docs/implementation/agent-profile-schema-boundary.md && rg -n "^## General Profile Data$|^## Deferred Connection Or Backend Data$|toolRefs|executionSettings|cwd|mcpServers|OpenCode|ACP" docs/implementation/agent-profile-schema-boundary.md</verify>
  <acceptance_criteria>
    - `docs/implementation/agent-profile-schema-boundary.md` names the shipped general profile fields and storage locations
    - the document contains `## General Profile Data` and `## Deferred Connection Or Backend Data` sections plus an explicit boundary table
    - the deferred section explicitly mentions `cwd`, `mcpServers`, capability flags, remote session IDs, and OpenCode or ACP-specific launch details as out of the Phase 2 general profile schema
    - the document references the `Agent` versus `Connection` terminology from `docs/domain-terminology.md`
    - the automated grep check passes
  </acceptance_criteria>
  <done>The repo has a durable schema and boundary reference that Phase 3 can build on without re-litigating the general profile contract.</done>
</task>

</tasks>

<threat_model>
trust_boundaries:
  - `apps/signal-foundry` chooses runtime persistence mode and startup behavior but must not bypass runtime storage and HTTP contracts.
  - Database migration on startup affects persistent runtime state and must be deterministic.
threat_register:
  - threat_id: T-2-03-01
    category: tampering
    component: apps/signal-foundry runtime wiring
    disposition: mitigate
    mitigation_plan: Reuse the existing `agentRuntime` storage selector and wire the profile service only through runtime contracts.
  - threat_id: T-2-03-02
    category: denial_of_service
    component: database startup migration path
    disposition: mitigate
    mitigation_plan: Run profile-service migration before serving requests when auto-migrate is enabled and cover enabled and disabled paths with tests.
  - threat_id: T-2-03-03
    category: integrity
    component: schema boundary documentation
    disposition: mitigate
    mitigation_plan: Publish an explicit profile-vs-connection contract so later phases do not silently reclassify backend-specific fields as general profile data.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd apps/signal-foundry && go test ./internal -run TestNewRuntime`
- [ ] `rg -n "AgentProfilesService|AutoMigrate" apps/signal-foundry/internal/runtime.go`
- [ ] `rg -n "^## General Profile Data$|^## Deferred Connection Or Backend Data$|toolRefs|executionSettings|cwd|mcpServers|OpenCode|ACP" docs/implementation/agent-profile-schema-boundary.md`
- [ ] `apps/signal-foundry/AGENTS.md` and `runtime/AGENTS.md` updated if runtime wiring, commands, or architecture guidance changed; otherwise summary explicitly states no AGENTS update was needed
- [ ] `make affected-lint-test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `apps/signal-foundry` starts with profile CRUD wired into the mounted runtime API
- Database-backed startup migrates saved profile storage alongside existing runtime persistence
- The repo contains a durable schema and boundary reference for Phase 2 agent profiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-profile-foundation/02-03-SUMMARY.md`
</output>

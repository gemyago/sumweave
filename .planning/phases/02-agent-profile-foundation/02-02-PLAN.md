---
phase: 02-agent-profile-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - runtime/internal/agentapi/openapi.yaml
  - runtime/internal/agentapi/api.gen.go
  - runtime/internal/agentapi/server.go
  - runtime/internal/agentapi/agent_profile_mapper.go
  - runtime/internal/agentapi/agent_profile_handlers.go
  - runtime/internal/agentapi/agent_profile_handlers_test.go
  - runtime/httpapi/handler.go
  - runtime/httpapi/handler_test.go
autonomous: true
requirements: [AGNT-01, AGNT-02, PERS-02]
user_setup: []
must_haves:
  truths:
    - The runtime HTTP API can list, create, read, update, and delete saved agent profiles through the same mounted runtime surface already used for sessions and providers.
    - The HTTP contract exposes only the general profile schema from Phase 2 and does not leak ACP or OpenCode connection details into the CRUD API.
    - The public `runtime/httpapi` wrapper remains thin and only wires dependencies into the generated/runtime handler layer.
  artifacts:
    - runtime/internal/agentapi/openapi.yaml
    - runtime/internal/agentapi/agent_profile_handlers.go
    - runtime/internal/agentapi/agent_profile_handlers_test.go
    - runtime/httpapi/handler.go
  key_links:
    - `runtime/internal/agentapi/openapi.yaml` generates new `/agent-profiles` routes and schemas into `runtime/internal/agentapi/api.gen.go`.
    - `runtime/httpapi/handler.go` must accept the new profile service dependency and pass it into `agentapi.NewAgentAPIServer`.
    - `runtime/internal/agentapi/agent_profile_handlers.go` maps HTTP requests to the `AgentProfilesService` introduced in Plan `02-01` and reuses the existing problem-details pattern.
---

<objective>
Expose runtime CRUD APIs for agent profiles through the OpenAPI and HTTP layers.

Purpose: Saved profiles need a control-plane surface so the harness can create, edit, and reuse them without bypassing the runtime API stack.
Output: New runtime OpenAPI endpoints, generated types and routes, handler implementations, tests, and thin HTTP wrapper wiring for profile CRUD.
</objective>

<execution_context>
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-agent-profile-foundation/02-CONTEXT.md
@.planning/phases/02-agent-profile-foundation/02-RESEARCH.md
@docs/implementation/opencode-acp-capability-map.md
@docs/domain-terminology.md
@./AGENTS.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
@runtime/httpapi/handler.go
@runtime/httpapi/handler_test.go
@runtime/internal/agentapi/openapi.yaml
@runtime/internal/agentapi/server.go
@runtime/internal/agentapi/provider_handlers.go
@runtime/internal/agentapi/provider_handlers_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the runtime OpenAPI contract for agent profile CRUD</name>
  <files>runtime/internal/agentapi/openapi.yaml, runtime/internal/agentapi/api.gen.go</files>
  <read_first>runtime/AGENTS.md, runtime/internal/agentapi/openapi.yaml, runtime/internal/agentapi/api.go, runtime/internal/agentapi/provider_handlers.go, docs/domain-terminology.md</read_first>
  <action>Update `runtime/internal/agentapi/openapi.yaml` to add profile CRUD endpoints that follow the existing provider and session conventions: `GET /agent-profiles`, `POST /agent-profiles`, `GET /agent-profiles/{profileName}`, `PUT /agent-profiles/{profileName}`, and `DELETE /agent-profiles/{profileName}`. Define request and response schemas in camelCase only, with a stable shape based on Plan `02-01`: `name`, optional `displayName`, `role`, `instructions`, `toolRefs`, `executionSettings`, `createdAt`, and `updatedAt`. Make `name` immutable by keeping it out of the update body. Do not add ACP or OpenCode binding fields to the API. Regenerate `runtime/internal/agentapi/api.gen.go` immediately after the spec change to stay aligned with the runtime module rule, but defer handler and server implementation to Task 2.</action>
  <verify>cd runtime && go generate ./internal/agentapi && rg -n "/agent-profiles|profileName|toolRefs|executionSettings" internal/agentapi/openapi.yaml internal/agentapi/api.gen.go</verify>
  <acceptance_criteria>
    - `runtime/internal/agentapi/openapi.yaml` contains `/agent-profiles` and `/agent-profiles/{profileName}` paths
    - the spec defines request and response schemas for agent profile CRUD using camelCase property names including `toolRefs` and `executionSettings`
    - generated route wrappers and types for agent profile CRUD exist in `runtime/internal/agentapi/api.gen.go`
    - no schema in `runtime/internal/agentapi/openapi.yaml` exposes ACP or OpenCode fields such as `cwd`, `mcpServers`, or runtime session identifiers
    - the generate and grep verification command exits 0
  </acceptance_criteria>
  <done>The runtime API contract and generated bindings include concrete CRUD routes for general agent profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement profile handlers and keep the public HTTP wrapper thin</name>
  <files>runtime/internal/agentapi/server.go, runtime/internal/agentapi/agent_profile_mapper.go, runtime/internal/agentapi/agent_profile_handlers.go, runtime/internal/agentapi/agent_profile_handlers_test.go, runtime/httpapi/handler.go, runtime/httpapi/handler_test.go</files>
  <read_first>runtime/AGENTS.md, docs/golang-coding-guide.md, runtime/internal/agentapi/server.go, runtime/internal/agentapi/provider_handlers.go, runtime/internal/agentapi/provider_handlers_test.go, runtime/httpapi/handler.go</read_first>
  <action>Generate `runtime/internal/agentapi/api.gen.go` from the updated spec, then add `AgentProfilesService` as a required dependency on `agentapi.ServerParams` and `httpapi.HandlerArgs`, mirroring the existing providers-service requirement. Implement `agent_profile_mapper.go` and `agent_profile_handlers.go` with the same division of responsibility used by provider CRUD: decode JSON, map request payloads to service params, call the service, map results to response DTOs, and translate known domain errors into `400`, `404`, `409`, or `204` as appropriate. Keep list ordering consistent with the service layer and reuse the existing problem-details helper instead of creating a second error format. In `runtime/httpapi/handler.go`, only validate the new dependency and pass it through to `agentapi.NewAgentAPIServer`; do not move CRUD logic into the public wrapper. Add focused tests covering list/create/get/update/delete success paths, malformed JSON, validation failures, duplicate names, missing profiles, and `httpapi.NewHandler` dependency validation.</action>
  <verify>cd runtime && go test ./internal/agentapi ./httpapi</verify>
  <acceptance_criteria>
    - `runtime/internal/agentapi/server.go` and `runtime/httpapi/handler.go` both require a non-nil profile service dependency
    - `runtime/internal/agentapi/agent_profile_handlers.go` implements CRUD handlers that follow the same problem-details and status-code conventions as provider handlers
    - `runtime/internal/agentapi/agent_profile_handlers_test.go` exercises all CRUD operations plus malformed JSON, validation, conflict, and not-found cases
    - `runtime/httpapi/handler_test.go` fails on nil profile service and succeeds when all runtime dependencies are present
    - `cd runtime && go test ./internal/agentapi ./httpapi` exits 0
  </acceptance_criteria>
  <done>Runtime profile CRUD is available over HTTP through the generated API layer, and the public wrapper remains a thin dependency injector.</done>
</task>

</tasks>

<threat_model>
trust_boundaries:
  - Authenticated runtime API callers can submit profile payloads that are mapped into persisted profile data.
  - `runtime/httpapi` remains a dependency-injection boundary and must not grow business logic.
threat_register:
  - threat_id: T-2-02-01
    category: tampering
    component: runtime/internal/agentapi profile CRUD
    disposition: mitigate
    mitigation_plan: Validate request payloads, keep immutable identifiers out of update bodies, and map domain errors into explicit HTTP failures.
  - threat_id: T-2-02-02
    category: information_disclosure
    component: OpenAPI schema and response mapping
    disposition: mitigate
    mitigation_plan: Keep the API schema limited to general profile fields and exclude ACP/OpenCode binding data from request and response types.
  - threat_id: T-2-02-03
    category: elevation_of_privilege
    component: runtime/httpapi wrapper
    disposition: mitigate
    mitigation_plan: Preserve the existing auth-protected runtime mount and keep handler logic inside `runtime/internal/agentapi` only.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd runtime && go generate ./internal/agentapi && go test ./internal/agentapi ./httpapi`
- [ ] `rg -n "/agent-profiles|profileName" runtime/internal/agentapi/openapi.yaml runtime/internal/agentapi/api.gen.go`
- [ ] `rg -n "AgentProfilesService" runtime/internal/agentapi/server.go runtime/httpapi/handler.go`
- [ ] `runtime/AGENTS.md` updated if the runtime HTTP surface or public contract changed; otherwise summary explicitly states no AGENTS update was needed
- [ ] `make affected-lint-test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime exposes agent profile CRUD through the same HTTP and API stack already used for sessions and providers
- The HTTP contract stays limited to the general profile schema from Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-profile-foundation/02-02-SUMMARY.md`
</output>

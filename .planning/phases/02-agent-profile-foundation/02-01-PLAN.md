---
phase: 02-agent-profile-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/agent/agent_profiles.go
  - runtime/agent/agent_profiles_test.go
  - runtime/internal/agentprofiles/agent_profiles.go
  - runtime/internal/agentprofiles/agent_profiles_test.go
  - runtime/internal/agentprofiles/file_agent_profiles_service.go
  - runtime/internal/agentprofiles/file_agent_profiles_service_test.go
  - runtime/internal/agentprofiles/db_agent_profiles_service.go
  - runtime/internal/agentprofiles/db_agent_profiles_service_test.go
autonomous: true
requirements: [AGNT-01, AGNT-03, PERS-01, PERS-02]
user_setup: []
must_haves:
  truths:
    - Sonalmod has a durable general-purpose agent profile model in `runtime` that stores role, instructions, tool references, and Sonalmod-owned execution settings without embedding ACP or OpenCode connection details.
    - Agent profiles can be created, listed, read, updated, and deleted through a runtime service that survives restart in both file-backed and database-backed storage modes.
    - The runtime exposes a stable public contract for agent profile persistence that `apps/sonalmod` can wire without reaching into `runtime/internal`.
  artifacts:
    - runtime/internal/agentprofiles/agent_profiles.go
    - runtime/internal/agentprofiles/file_agent_profiles_service.go
    - runtime/internal/agentprofiles/db_agent_profiles_service.go
    - runtime/agent/agent_profiles.go
  key_links:
    - `runtime/agent/agent_profiles.go` re-exports the profile contract and constructors from `runtime/internal/agentprofiles` the same way provider config services are exposed today.
    - File persistence writes canonical YAML files under `{baseDir}/agent-profiles/{name}.yaml` and reload tests construct a fresh service instance against the same directory.
    - Database persistence uses the existing GORM/table-prefix conventions and exposes `AutoMigrate()` on the profile service so the app layer can migrate without importing internal packages.
---

<objective>
Build the runtime profile domain and persistence layer for general agent profiles.

Purpose: Phase 2 needs a durable, backend-agnostic profile model before API exposure or OpenCode-specific bindings can be added.
Output: A new `runtime/internal/agentprofiles` package with file and database storage, tests, and a public `runtime/agent` contract for profile services.
</objective>

<execution_context>
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-agent-profile-foundation/02-CONTEXT.md
@.planning/phases/02-agent-profile-foundation/02-RESEARCH.md
@docs/implementation/opencode-acp-capability-map.md
@docs/domain-terminology.md
@./AGENTS.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
@runtime/agent/providers_config.go
@runtime/internal/llmproviders/providers_config.go
@runtime/internal/llmproviders/file_providers_config_service.go
@runtime/internal/llmproviders/db_providers_config_service.go
@runtime/internal/sessions/factory.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the agent profile contract and runtime public surface</name>
  <files>runtime/internal/agentprofiles/agent_profiles.go, runtime/internal/agentprofiles/agent_profiles_test.go, runtime/agent/agent_profiles.go, runtime/agent/agent_profiles_test.go</files>
  <read_first>AGENTS.md, runtime/AGENTS.md, docs/golang-coding-guide.md, docs/domain-terminology.md, runtime/agent/providers_config.go, runtime/internal/llmproviders/providers_config.go</read_first>
  <action>Create a new `runtime/internal/agentprofiles` package that mirrors the provider-config domain pattern but stays specific to agent profiles. Define `AgentProfile`, `CreateAgentProfileParams`, `UpdateAgentProfileParams`, explicit errors (`ErrAgentProfileNotFound`, `ErrAgentProfileNameConflict`), and an `AgentProfilesService` interface. Make the primary identifier an immutable technical `Name` matching the existing provider-name convention `^[a-z][a-z0-9-]*$`; add optional `DisplayName`, required `Role`, required `Instructions`, `ToolRefs []string`, `ExecutionSettings`, `CreatedAt`, and `UpdatedAt`. Keep `ExecutionSettings` intentionally small and Sonalmod-owned by storing only the settings the control plane can interpret in this phase, starting with a default fully-qualified model field; do not add `cwd`, `mcpServers`, ACP capability flags, remote session IDs, or OpenCode connection metadata. Put lightweight validation in the domain layer: trim and require role and instructions, reject empty tool refs, dedupe tool refs while preserving order, and keep update operations from mutating `Name` or collapsing the general-profile boundary. In `runtime/agent/agent_profiles.go`, expose thin public aliases for the profile types and service interface so embedder code can use the contract without importing `internal` packages.</action>
  <verify>cd runtime && go test ./agent ./internal/agentprofiles</verify>
  <acceptance_criteria>
    - `runtime/internal/agentprofiles/agent_profiles.go` defines an `AgentProfilesService` interface with CRUD methods and `AutoMigrate() error`
    - `runtime/internal/agentprofiles/agent_profiles.go` contains no ACP- or OpenCode-specific fields such as `cwd`, `mcpServers`, capability flags, or remote session identifiers
    - `runtime/agent/agent_profiles.go` exports aliases for the profile types and service interface
    - `runtime/internal/agentprofiles/agent_profiles_test.go` covers identifier validation, tool ref normalization, and immutable-name update rules
    - `cd runtime && go test ./agent ./internal/agentprofiles` exits 0
  </acceptance_criteria>
  <done>The runtime has a stable public contract for general agent profiles and the domain boundary is enforced by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement file and database persistence with restart-shaped coverage</name>
  <files>runtime/internal/agentprofiles/file_agent_profiles_service.go, runtime/internal/agentprofiles/file_agent_profiles_service_test.go, runtime/internal/agentprofiles/db_agent_profiles_service.go, runtime/internal/agentprofiles/db_agent_profiles_service_test.go</files>
  <read_first>runtime/AGENTS.md, docs/golang-coding-guide.md, runtime/internal/llmproviders/file_providers_config_service.go, runtime/internal/llmproviders/db_providers_config_service.go, runtime/internal/sessions/factory.go</read_first>
  <action>Implement `FileAgentProfilesService` and `DatabaseAgentProfilesService` following the existing provider-config storage patterns instead of introducing a new persistence shape. File storage must use canonical YAML files under `{baseDir}/agent-profiles/{name}.yaml`, return profiles sorted by `CreatedAt` ascending, and preserve `CreatedAt` while updating only mutable fields plus `UpdatedAt`. Database storage must use GORM with an explicit `agent_profiles` table model, explicit column names, JSON serialization for `toolRefs` and `executionSettings`, and the existing table-prefix behavior used elsewhere in runtime. Make `AutoMigrate()` a no-op for file storage and a real table migration for the DB service so `apps/sonalmod` can orchestrate migrations without type assertions. In `runtime/agent/agent_profiles.go`, add the public constructors `NewFileAgentProfilesService` and `NewDatabaseAgentProfilesService` once the concrete services exist. Add tests that prove create/get/list/update/delete behavior, duplicate-name conflicts, not-found handling, update preservation of unrelated fields, file-backed restart reloads, and DB-backed restart reloads against `:memory:`.</action>
  <verify>cd runtime && go test ./internal/agentprofiles ./agent</verify>
  <acceptance_criteria>
    - `runtime/internal/agentprofiles/file_agent_profiles_service.go` writes YAML files under an `agent-profiles` subdirectory and reads them back in created order
    - `runtime/internal/agentprofiles/db_agent_profiles_service.go` defines a GORM model with explicit column names and an `AutoMigrate()` implementation
    - `runtime/agent/agent_profiles.go` exports `NewFileAgentProfilesService` and `NewDatabaseAgentProfilesService` once the concrete services exist
    - file and database tests each include a restart-shaped case that creates a profile, constructs a fresh service against the same backing store, and loads the same profile back unchanged
    - update tests prove mutable fields change while `Name` and `CreatedAt` stay stable
    - `cd runtime && go test ./internal/agentprofiles ./agent` exits 0
  </acceptance_criteria>
  <done>Runtime profile persistence works in both supported storage modes and is covered by restart-sensitive tests.</done>
</task>

</tasks>

<threat_model>
trust_boundaries:
  - The runtime accepts profile payloads from authenticated control-plane callers and persists them to file or database storage.
  - Public `runtime/agent` constructors must not expose `runtime/internal` implementation details.
threat_register:
  - threat_id: T-2-01-01
    category: tampering
    component: runtime/internal/agentprofiles
    disposition: mitigate
    mitigation_plan: Validate profile fields, keep identifiers immutable, and reject ACP-specific fields from the general schema.
  - threat_id: T-2-01-02
    category: information_disclosure
    component: runtime/agent public contract
    disposition: mitigate
    mitigation_plan: Re-export only stable aliases and constructors; keep storage internals and backend-specific fields out of the public surface.
  - threat_id: T-2-01-03
    category: integrity
    component: file and database persistence
    disposition: mitigate
    mitigation_plan: Cover restart-shaped persistence, duplicate conflicts, and update preservation with deterministic tests.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd runtime && go test ./agent ./internal/agentprofiles`
- [ ] `! rg -n "mcpServers|cwd|session(Id|ID)|capabilit" runtime/internal/agentprofiles/agent_profiles.go`
- [ ] `rg -n "type AgentProfilesService|type AgentProfile" runtime/agent/agent_profiles.go`
- [ ] `runtime/AGENTS.md` updated if the runtime public contract or persistence guidance changed; otherwise summary explicitly states no AGENTS update was needed
- [ ] `make affected-lint-test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime has a durable general profile model and persistence layer that stays separate from ACP/OpenCode connection details
- File-backed and database-backed profile services both survive restart and preserve saved definitions
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-profile-foundation/02-01-SUMMARY.md`
</output>

---
phase: 03-opencode-coding-lane
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - runtime/internal/codinglane/opencode_acp_client.go
  - runtime/internal/codinglane/opencode_acp_client_test.go
  - runtime/internal/codinglane/opencode_launcher.go
  - runtime/internal/codinglane/opencode_launcher_test.go
  - runtime/internal/codinglane/launch_request_mapper.go
  - runtime/internal/codinglane/launch_request_mapper_test.go
autonomous: true
requirements: [CODE-03]
user_setup:
  - service: opencode
    why: "Launch OpenCode ACP subprocess for coding runs"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - The harness can launch an OpenCode-backed coding run from saved binding + profile data.
    - Launch flow uses only validated ACP behavior from Phase 1.
    - Launch failures are surfaced with actionable errors.
  artifacts:
    - runtime/internal/codinglane/opencode_acp_client.go
    - runtime/internal/codinglane/opencode_launcher.go
    - runtime/internal/codinglane/launch_request_mapper.go
  key_links:
    - opencode launcher resolves saved binding and profile, then starts ACP session with validated method set only.
    - launcher returns structured result/error that API handlers can map directly.
---

<objective>
Implement the OpenCode ACP launch path for coding runs.

Purpose: satisfy CODE-03 using the validated ACP subset from Phase 1.
Output: runtime coding-lane ACP client + launcher service with deterministic tests and clear error mapping.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-opencode-coding-lane/03-CONTEXT.md
@.planning/phases/03-opencode-coding-lane/03-RESEARCH.md
@.planning/phases/03-opencode-coding-lane/03-01-PLAN.md
@docs/implementation/opencode-acp-capability-map.md
@tests/AGENTS.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build validated-subset ACP transport for OpenCode launches</name>
  <files>runtime/internal/codinglane/opencode_acp_client.go, runtime/internal/codinglane/opencode_acp_client_test.go</files>
  <behavior>
    - Test 1: ACP client performs initialize -> session/new -> session/prompt and consumes session/update messages.
    - Test 2: client never calls session/cancel, session/close, session/load, or session/list in normal launch path.
    - Test 3: malformed or missing ACP responses return explicit typed errors.
  </behavior>
  <action>Implement an ACP stdio client dedicated to OpenCode launches per D-02. Reuse protocol framing patterns validated in Phase 1 summaries, but keep production code in runtime (not tests module). Require `protocolVersion` in initialize params; send `session/new` with explicit `cwd` and `mcpServers`; send prompt as text block array; process `session/update` for progress/final events. Do not depend on cancellation/resume/listing/close features (D-02, D-01).</action>
  <verify>
    <automated>cd runtime && go test ./internal/codinglane -run TestOpenCodeACPClient</automated>
  </verify>
  <done>Runtime has tested ACP transport for the validated OpenCode subset only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement launcher service that resolves saved configuration and reports failures clearly</name>
  <files>runtime/internal/codinglane/opencode_launcher.go, runtime/internal/codinglane/opencode_launcher_test.go, runtime/internal/codinglane/launch_request_mapper.go, runtime/internal/codinglane/launch_request_mapper_test.go</files>
  <behavior>
    - Test 1: launch succeeds when profile + binding exist and ACP client returns completion.
    - Test 2: missing profile or missing binding returns not-found errors.
    - Test 3: subprocess/ACP failures are mapped to clear launch-failed errors with diagnostic context.
  </behavior>
  <action>Create launcher orchestration that accepts a profile selector + prompt input, loads profile and OpenCode binding, composes launch params, invokes ACP client, and returns structured launch result for API handlers (D-01, D-03). Add mapper logic to merge profile instructions/toolRefs/default model with binding-side launch defaults without moving connection fields into profile schema (D-03). Ensure error types distinguish validation, not-found, and subprocess/protocol failures for debuggability (CODE-03).</action>
  <verify>
    <automated>cd runtime && go test ./internal/codinglane -run TestOpenCodeACPLauncher</automated>
  </verify>
  <done>Launch workflow is executable, deterministic under tests, and provides actionable failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API launch request -> launcher | untrusted prompt/selectors can trigger subprocess work |
| launcher -> opencode subprocess | external process I/O and protocol data is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | ACP request construction | mitigate | Strictly construct only validated methods/fields and validate request inputs before spawn |
| T-03-05 | I | ACP response handling | mitigate | Parse/validate JSON-RPC envelopes and return typed protocol errors for malformed data |
| T-03-06 | D | subprocess launch path | mitigate | Bound launch flow with context/cancellation and return immediate typed failures for spawn/protocol errors |
</threat_model>

<verification>
- `cd runtime && go test ./internal/codinglane -run TestOpenCodeACPClient`
- `cd runtime && go test ./internal/codinglane -run TestOpenCodeACPLauncher`
- `! rg -n "session/cancel|session/close|session/list|session/load" runtime/internal/codinglane/opencode_acp_client.go`
- `make affected-lint-test`
</verification>

<success_criteria>
- CODE-03 launch path works from saved data and validated ACP calls only.
- Failure modes are explicit enough to debug launch/configuration/protocol issues.
</success_criteria>

<output>
After completion, create `.planning/phases/03-opencode-coding-lane/03-02-SUMMARY.md`
</output>


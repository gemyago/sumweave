---
phase: 03-opencode-coding-lane
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - runtime/internal/agentapi/openapi.yaml
  - runtime/internal/agentapi/api.gen.go
  - runtime/internal/agentapi/opencode_binding_mapper.go
  - runtime/internal/agentapi/opencode_binding_handlers.go
  - runtime/internal/agentapi/opencode_binding_handlers_test.go
  - runtime/internal/agentapi/opencode_launch_handlers.go
  - runtime/internal/agentapi/opencode_launch_handlers_test.go
  - runtime/internal/agentapi/server.go
  - runtime/httpapi/handler.go
  - runtime/httpapi/handler_test.go
  - apps/signal-foundry/internal/runtime.go
  - apps/signal-foundry/internal/runtime_test.go
  - docs/implementation/opencode-coding-lane-contract.md
autonomous: true
requirements: [CODE-02, CODE-03, CODE-04]
user_setup:
  - service: opencode
    why: "Live launch verification of OpenCode ACP path"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - Users can define/select saved OpenCode coding bindings via runtime API.
    - Users can launch OpenCode coding runs from saved profile+binding selectors without re-entering full configuration each time.
    - Launch/API failures are returned clearly through runtime HTTP responses.
  artifacts:
    - runtime/internal/agentapi/openapi.yaml
    - runtime/internal/agentapi/opencode_binding_handlers.go
    - runtime/internal/agentapi/opencode_launch_handlers.go
    - apps/signal-foundry/internal/runtime.go
  key_links:
    - API handlers call runtime codinglane services introduced in 03-01 and 03-02.
    - apps/signal-foundry runtime wiring injects codinglane services into httpapi handler dependency graph.
---

<objective>
Expose OpenCode coding-lane config and launch flows through runtime API and app wiring.

Purpose: complete CODE-02/CODE-03/CODE-04 with executable control-plane entrypoints.
Output: OpenAPI endpoints, handlers/tests, app wiring, and implementation contract documentation.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-opencode-coding-lane/03-CONTEXT.md
@.planning/phases/03-opencode-coding-lane/03-RESEARCH.md
@.planning/phases/03-opencode-coding-lane/03-01-PLAN.md
@.planning/phases/03-opencode-coding-lane/03-02-PLAN.md
@runtime/AGENTS.md
@apps/signal-foundry/AGENTS.md
@docs/golang-coding-guide.md
@runtime/internal/agentapi/openapi.yaml
@runtime/httpapi/handler.go
@apps/signal-foundry/internal/runtime.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime OpenAPI and handlers for OpenCode binding CRUD and launch</name>
  <files>runtime/internal/agentapi/openapi.yaml, runtime/internal/agentapi/api.gen.go, runtime/internal/agentapi/opencode_binding_mapper.go, runtime/internal/agentapi/opencode_binding_handlers.go, runtime/internal/agentapi/opencode_binding_handlers_test.go, runtime/internal/agentapi/opencode_launch_handlers.go, runtime/internal/agentapi/opencode_launch_handlers_test.go, runtime/internal/agentapi/server.go, runtime/httpapi/handler.go, runtime/httpapi/handler_test.go</files>
  <behavior>
    - Test 1: CRUD endpoints for OpenCode bindings succeed and enforce validation/conflict/not-found semantics.
    - Test 2: Launch endpoint accepts selector + prompt and invokes launcher service.
    - Test 3: launch and binding errors map to deterministic HTTP status/problem-details responses.
  </behavior>
  <action>Extend OpenAPI spec and generated bindings for OpenCode binding CRUD and launch operations per D-01 and D-05. Keep JSON keys camelCase and generate immediately after spec edits. Implement handlers using established provider/profile handler conventions (thin mapping + service calls + problem-details errors). Require new services on `agentapi.ServerParams` and `httpapi.HandlerArgs`, keeping wrapper logic thin per D-04. Ensure launch requests rely on saved configuration references rather than full config payload re-entry (CODE-04).</action>
  <verify>
    <automated>cd runtime && go generate ./internal/agentapi && go test ./internal/agentapi ./httpapi</automated>
  </verify>
  <done>Runtime HTTP surface exposes complete OpenCode binding management and launch endpoints with robust handler tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire coding-lane services in app runtime and publish integration contract</name>
  <files>apps/signal-foundry/internal/runtime.go, apps/signal-foundry/internal/runtime_test.go, docs/implementation/opencode-coding-lane-contract.md</files>
  <action>Wire OpenCode binding and launcher services in `apps/signal-foundry/internal/runtime.go` using existing storage selector and runtime dependency construction patterns (D-04). Ensure DB auto-migrate includes OpenCode binding tables when enabled. Add runtime tests for file and database startup covering new dependencies. Create `docs/implementation/opencode-coding-lane-contract.md` summarizing endpoint contracts, persisted boundary (general profile vs OpenCode connection binding), validated ACP subset used, and explicit deferred ACP features per D-02 and D-03.</action>
  <verify>
    <automated>cd apps/signal-foundry && go test ./internal -run TestNewRuntime</automated>
  </verify>
  <done>App startup can serve OpenCode coding-lane APIs and the repo has a durable contract for this integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external client -> runtime API | untrusted JSON requests for binding/launch |
| app runtime wiring -> launcher subprocess path | injected dependencies can alter launch behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | S | OpenCode launch endpoint | mitigate | Require authenticated runtime API path and explicit selector validation before launch |
| T-03-08 | T | Binding CRUD handlers | mitigate | Validate payloads and map domain conflicts/not-found consistently; no direct DB writes in handlers |
| T-03-09 | I | HTTP error responses | mitigate | Return problem-details without leaking sensitive process internals while preserving actionable diagnostics |
</threat_model>

<verification>
- `cd runtime && go generate ./internal/agentapi && go test ./internal/agentapi ./httpapi`
- `cd apps/signal-foundry && go test ./internal -run TestNewRuntime`
- `rg -n "OpenCode|ACP|validated|deferred" docs/implementation/opencode-coding-lane-contract.md`
- `make affected-lint-test`
</verification>

<success_criteria>
- Users can save/select OpenCode coding bindings and launch coding runs from saved configuration.
- API and app wiring are complete and consistent with existing runtime architecture.
- Documentation captures exactly what ACP behavior is used and what remains deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/03-opencode-coding-lane/03-03-SUMMARY.md`
</output>


---
phase: 03-opencode-coding-lane
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/agent/opencode_bindings.go
  - runtime/agent/opencode_bindings_test.go
  - runtime/internal/codinglane/opencode_binding.go
  - runtime/internal/codinglane/opencode_binding_test.go
  - runtime/internal/codinglane/file_opencode_binding_service.go
  - runtime/internal/codinglane/file_opencode_binding_service_test.go
  - runtime/internal/codinglane/db_opencode_binding_service.go
  - runtime/internal/codinglane/db_opencode_binding_service_test.go
autonomous: true
requirements: [CODE-02, CODE-04]
user_setup: []
must_haves:
  truths:
    - A saved general agent profile can be linked to an OpenCode ACP coding-lane binding without changing the Phase 2 profile schema.
    - OpenCode binding data persists across restart in both file and database storage modes.
    - Saved binding defaults can be reused for subsequent runs so full coding-lane config is not re-entered each time.
  artifacts:
    - runtime/internal/codinglane/opencode_binding.go
    - runtime/internal/codinglane/file_opencode_binding_service.go
    - runtime/internal/codinglane/db_opencode_binding_service.go
    - runtime/agent/opencode_bindings.go
  key_links:
    - runtime/agent/opencode_bindings.go exposes aliases/constructors from runtime/internal/codinglane.
    - Binding records reference existing profile names and store backend-specific connection defaults separately.
---

<objective>
Create OpenCode connection/binding persistence for coding profiles.

Purpose: satisfy CODE-02 and CODE-04 while preserving the Phase 2 general profile boundary.
Output: runtime domain + persistence services for saved OpenCode coding-lane bindings.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-opencode-coding-lane/03-CONTEXT.md
@.planning/phases/03-opencode-coding-lane/03-RESEARCH.md
@.planning/phases/02-agent-profile-foundation/02-01-SUMMARY.md
@docs/implementation/agent-profile-schema-boundary.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
@runtime/agent/agent_profiles.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define OpenCode binding contracts and public runtime aliases</name>
  <files>runtime/internal/codinglane/opencode_binding.go, runtime/internal/codinglane/opencode_binding_test.go, runtime/agent/opencode_bindings.go, runtime/agent/opencode_bindings_test.go</files>
  <behavior>
    - Test 1: Creating a binding with invalid profile reference or invalid binding name fails validation.
    - Test 2: Updating a binding keeps immutable identifier fields stable.
    - Test 3: Binding schema contains backend-side fields only and excludes general profile fields.
  </behavior>
  <action>Create `OpenCodeBinding` domain types and `OpenCodeBindingService` interface in `runtime/internal/codinglane` per D-03 and D-04. Keep general profile fields out of this schema; include only connection-side data needed for OpenCode ACP launch defaults (for example profile reference, cwd default, agent command/args, and launch options constrained by D-02). Add explicit errors for not found and conflict. In `runtime/agent/opencode_bindings.go`, re-export thin aliases and constructors following runtime provider/profile patterns (D-04).</action>
  <verify>
    <automated>cd runtime && go test ./agent ./internal/codinglane</automated>
  </verify>
  <done>Runtime has a stable OpenCode binding contract separated from general profiles, with tests enforcing boundary and validation rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement file/DB binding persistence with restart coverage</name>
  <files>runtime/internal/codinglane/file_opencode_binding_service.go, runtime/internal/codinglane/file_opencode_binding_service_test.go, runtime/internal/codinglane/db_opencode_binding_service.go, runtime/internal/codinglane/db_opencode_binding_service_test.go</files>
  <behavior>
    - Test 1: create/get/list/update/delete works in file mode and DB mode.
    - Test 2: duplicate binding names return conflict error.
    - Test 3: restart-shaped reload preserves saved bindings for both storage backends.
  </behavior>
  <action>Implement file and database services mirroring Phase 2 persistence patterns per D-04. File mode stores YAML under `{baseDir}/opencode-bindings/{name}.yaml`; DB mode uses explicit GORM model/table names and `AutoMigrate()`. Add list ordering and timestamp behavior consistent with profile services. Ensure these persisted bindings can be resolved by profile reference to support CODE-04 selection without re-entry (D-01).</action>
  <verify>
    <automated>cd runtime && go test ./internal/codinglane ./agent</automated>
  </verify>
  <done>OpenCode bindings persist and reload across restarts in both supported storage modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control-plane API -> binding service | untrusted config input crosses into persisted connection data |
| app/runtime wiring -> storage backends | persisted backend config can affect external process launch behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `runtime/internal/codinglane/*` | mitigate | Validate binding fields, keep identifier immutability, reject malformed command/arg defaults |
| T-03-02 | I | `runtime/agent/opencode_bindings.go` | mitigate | Expose only stable aliases/constructors; keep internal storage structs hidden |
| T-03-03 | D | file/DB services | mitigate | Add restart and conflict tests to prevent corruption and unstable selection behavior |
</threat_model>

<verification>
- `cd runtime && go test ./agent ./internal/codinglane`
- `! rg -n "instructions|toolRefs|executionSettings" runtime/internal/codinglane/opencode_binding.go`
- `make affected-lint-test`
</verification>

<success_criteria>
- CODE-02 and CODE-04 prerequisites are in place through durable OpenCode bindings.
- Phase 2 general profile schema remains unchanged and separate from connection data.
</success_criteria>

<output>
After completion, create `.planning/phases/03-opencode-coding-lane/03-01-SUMMARY.md`
</output>


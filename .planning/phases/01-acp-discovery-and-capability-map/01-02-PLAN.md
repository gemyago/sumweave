---
phase: 01-acp-discovery-and-capability-map
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/implementation/opencode-acp-experiments.md
  - docs/implementation/opencode-acp-capability-map.md
  - docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl
  - docs/implementation/opencode-acp-transcripts/cancel.jsonl
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CODE-01]
user_setup:
  - Install `opencode` so the `opencode` binary is on `PATH`.
  - Authenticate or configure OpenCode so `opencode acp` can start successfully in the local environment.
must_haves:
  truths:
    - Signal Foundry has an OpenCode capability map based on observed ACP transcripts rather than assumptions.
    - Unsupported or unvalidated ACP features are documented as deferred or out of scope for the first OpenCode integration.
    - Project planning docs reflect the validated ACP subset discovered in this phase.
  artifacts:
    - docs/implementation/opencode-acp-experiments.md
    - docs/implementation/opencode-acp-capability-map.md
    - docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl
  key_links:
    - docs/implementation/opencode-acp-capability-map.md cites transcript files under docs/implementation/opencode-acp-transcripts
    - .planning/PROJECT.md and .planning/ROADMAP.md are revised to match observed ACP findings
---

<objective>
Run the ACP probe against OpenCode, publish the capability map, and feed the findings back into project planning.

Purpose: The first real OpenCode integration should be based on observed ACP behavior instead of memory or guesswork.
Output: Stored OpenCode ACP transcripts, experiment notes, a capability map, and updated planning docs.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-acp-discovery-and-capability-map/01-CONTEXT.md
@.planning/phases/01-acp-discovery-and-capability-map/01-RESEARCH.md
@.planning/phases/01-acp-discovery-and-capability-map/01-VALIDATION.md
@.planning/phases/01-acp-discovery-and-capability-map/01-01-SUMMARY.md
@tests/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run scripted OpenCode ACP experiments and store transcripts</name>
  <files>docs/implementation/opencode-acp-experiments.md, docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl, docs/implementation/opencode-acp-transcripts/cancel.jsonl</files>
  <read_first>tests/AGENTS.md, .planning/phases/01-acp-discovery-and-capability-map/01-RESEARCH.md, tests/agent/integration-cli/main.go, tests/agent/integration-cli/acp_client.go</read_first>
  <action>Use the ACP mode added to `tests/agent/integration-cli` in Plan `01-01` to run a minimal OpenCode ACP scenario set against `opencode acp`. Capture at least an initialize-plus-prompt transcript and a cancellation transcript under `docs/implementation/opencode-acp-transcripts/`. In `docs/implementation/opencode-acp-experiments.md`, document the exact commands used, required environment assumptions, observed capability flags, session lifecycle behavior, and whether `session/load`, `session/list`, or `session/close` were advertised or absent. If a capability is not advertised, record it as not validated and do not try to force it into the first integration shape.</action>
  <verify>test -f docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl && test -f docs/implementation/opencode-acp-transcripts/cancel.jsonl</verify>
  <acceptance_criteria>
    - `docs/implementation/opencode-acp-experiments.md` contains the string `opencode acp`
    - `docs/implementation/opencode-acp-experiments.md` contains the string `initialize`
    - `docs/implementation/opencode-acp-experiments.md` contains the string `session/prompt`
    - `docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl` exists
    - `docs/implementation/opencode-acp-transcripts/cancel.jsonl` exists
  </acceptance_criteria>
  <done>OpenCode ACP transcripts are stored in-repo and the experiment procedure is reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the capability map and update planning docs</name>
  <files>docs/implementation/opencode-acp-capability-map.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, docs/implementation/opencode-acp-experiments.md, docs/domain-terminology.md</read_first>
  <action>Create `docs/implementation/opencode-acp-capability-map.md` with a table covering the ACP features Signal Foundry cares about most: capability negotiation, session creation, prompt streaming, cancellation, session resume, session listing, session close, MCP server injection, slash-command limitations, and any permission or tool-related signals actually observed. Each row must classify the feature as `validated`, `advertised but untested`, `not advertised`, or `deferred`. Then update `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` so the next phase starts from the validated ACP subset instead of generic assumptions.</action>
  <verify>rg -n "validated|advertised but untested|not advertised|deferred" docs/implementation/opencode-acp-capability-map.md && rg -n "validated ACP subset|capability map|OpenCode" .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md</verify>
  <acceptance_criteria>
    - `docs/implementation/opencode-acp-capability-map.md` contains the strings `validated`, `advertised but untested`, `not advertised`, and `deferred`
    - `docs/implementation/opencode-acp-capability-map.md` references transcript files under `docs/implementation/opencode-acp-transcripts`
    - `.planning/PROJECT.md` contains the string `validated ACP subset` or an equivalent explicit phrase
    - `.planning/ROADMAP.md` mentions OpenCode planning based on observed protocol behavior
    - `.planning/STATE.md` reflects that ACP findings, not assumptions, drive the next phase
  </acceptance_criteria>
  <done>The project has a concrete OpenCode capability map and the planning docs have been updated to match it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/implementation/opencode-acp-transcripts/initialize-and-prompt.jsonl`
- [ ] `test -f docs/implementation/opencode-acp-transcripts/cancel.jsonl`
- [ ] `rg -n "validated|advertised but untested|not advertised|deferred" docs/implementation/opencode-acp-capability-map.md`
- [ ] `make affected-lint-test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OpenCode ACP findings are captured as durable transcripts and docs
- The next project phase can design around validated ACP behavior instead of assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/01-acp-discovery-and-capability-map/01-02-SUMMARY.md`
</output>

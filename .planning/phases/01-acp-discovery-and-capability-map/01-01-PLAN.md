---
phase: 01-acp-discovery-and-capability-map
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent/integration-cli/main.go
  - tests/agent/integration-cli/acp_cmd.go
  - tests/agent/integration-cli/acp_client.go
  - tests/agent/integration-cli/acp_transcript.go
  - tests/agent/integration-cli/acp_client_test.go
  - tests/agent/integration-cli/acp_transcript_test.go
  - tests/AGENTS.md
autonomous: true
requirements: [CODE-01]
user_setup: []
must_haves:
  truths:
    - Signal Foundry has an ACP mode inside `tests/agent/integration-cli` that can negotiate ACP capabilities over stdio and execute the minimum session lifecycle needed for OpenCode experiments.
    - The integration CLI ACP mode captures raw JSON-RPC requests, responses, and notifications to transcript files without relying on PTY scraping.
  artifacts:
    - tests/agent/integration-cli/acp_cmd.go
    - tests/agent/integration-cli/acp_client.go
    - tests/agent/integration-cli/acp_client_test.go
    - tests/AGENTS.md
  key_links:
    - tests/agent/integration-cli/main.go wires an ACP subcommand implemented in tests/agent/integration-cli/acp_cmd.go.
    - tests/agent/integration-cli/acp_transcript.go is exercised by tests/agent/integration-cli/acp_transcript_test.go.
    - tests/AGENTS.md documents how to run the probe against a real ACP agent.
---

<objective>
Build a reusable ACP probe harness inside the repo for OpenCode experiments.

Purpose: Signal Foundry needs real ACP evidence before it can design a stable OpenCode integration.
Output: A Go-based ACP mode inside the existing integration CLI, transcript capture support, tests, and operator instructions.
</objective>

<execution_context>
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/signal-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-acp-discovery-and-capability-map/01-CONTEXT.md
@.planning/phases/01-acp-discovery-and-capability-map/01-RESEARCH.md
@./AGENTS.md
@tests/AGENTS.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
@tests/agent/integration-cli/main.go
@tests/agent/integration-cli/cli.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend integration-cli with an ACP subcommand and transport client</name>
  <files>tests/agent/integration-cli/main.go, tests/agent/integration-cli/acp_cmd.go, tests/agent/integration-cli/acp_client.go</files>
  <read_first>AGENTS.md, tests/AGENTS.md, runtime/AGENTS.md, docs/golang-coding-guide.md, tests/agent/integration-cli/main.go, tests/agent/integration-cli/cli.go</read_first>
  <action>Extend the existing `tests/agent/integration-cli` binary with a dedicated ACP subcommand instead of creating a new module. Add a CLI path that accepts `--agent-command`, repeated `--agent-arg`, `--cwd`, `--prompt`, `--transcript`, `--load-session`, and `--cancel-after`. In `acp_client.go`, implement newline-delimited JSON-RPC stdio transport that launches the downstream subprocess, sends `initialize`, reads the negotiated capabilities, runs `session/new` or `session/load` as appropriate, sends `session/prompt`, and optionally sends `session/cancel` after the requested delay. Treat capability-gated methods like `session/load` as conditional on the advertised capability flags instead of assuming support.</action>
  <verify>cd tests/agent/integration-cli && go test ./...</verify>
  <acceptance_criteria>
    - `tests/agent/integration-cli/main.go` contains an ACP subcommand or ACP-specific flag wiring
    - `tests/agent/integration-cli/acp_client.go` contains JSON-RPC method strings `initialize`, `session/new`, `session/prompt`, and `session/cancel`
    - `tests/agent/integration-cli/acp_client.go` checks agent capabilities before attempting `session/load`
    - `cd tests/agent/integration-cli && go test ./...` exits 0
  </acceptance_criteria>
  <done>Integration CLI has a runnable ACP path and the lifecycle code is covered by the existing module test command.</done>
</task>

<task type="auto">
  <name>Task 2: Add ACP transcript capture, tests, and run instructions</name>
  <files>tests/agent/integration-cli/acp_transcript.go, tests/agent/integration-cli/acp_client_test.go, tests/agent/integration-cli/acp_transcript_test.go, tests/AGENTS.md</files>
  <read_first>AGENTS.md, tests/AGENTS.md, docs/golang-coding-guide.md, tests/agent/integration-cli/cli_test.go</read_first>
  <action>Add transcript recording that writes every outbound request and inbound response or notification as JSON lines to the file passed by `--transcript`. Cover the ACP path with tests for newline framing, capability parsing, transcript writing, and cancellation request emission. Update `tests/AGENTS.md` with a short section for the integration CLI ACP mode that explains the expected manual setup, the command shape, and the fact that the user must have `opencode` installed and authenticated before real ACP experiments run.</action>
  <verify>cd tests/agent/integration-cli && go test ./...</verify>
  <acceptance_criteria>
    - `tests/agent/integration-cli/acp_transcript.go` contains logic to write both request and response envelopes
    - `tests/agent/integration-cli/acp_client_test.go` contains a test covering capability-gated session loading
    - `tests/agent/integration-cli/acp_transcript_test.go` exits successfully under `go test`
    - `tests/AGENTS.md` contains the string `integration-cli`
    - `tests/AGENTS.md` contains the string `opencode acp`
  </acceptance_criteria>
  <done>ACP transcripts are test-covered and operators have repo-local instructions for real ACP runs through the existing integration CLI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd tests/agent/integration-cli && go test ./...`
- [ ] `rg -n "integration-cli|opencode acp" tests/AGENTS.md`
- [ ] `rg -n "initialize|session/new|session/prompt|session/cancel" tests/agent/integration-cli/acp_client.go`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Signal Foundry has a reusable ACP path inside the existing integration CLI ready for real OpenCode experiments
</success_criteria>

<output>
After completion, create `.planning/phases/01-acp-discovery-and-capability-map/01-01-SUMMARY.md`
</output>

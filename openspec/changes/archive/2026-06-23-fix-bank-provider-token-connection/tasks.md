## 1. Finance Provider Linking Contract

- [x] 1.1 Add explicit supported bank-provider mapping for `monobank` and `pko`, and must follow TDD flow by first writing failing finance service tests proving monobank token linking is accepted, token linking for PKO is rejected, PKO redirect/SCA linking maps to Enable Banking, monobank redirect linking is rejected, and unsupported provider/method errors are bounded and secret-free before implementing and verifying focused tests.
- [x] 1.2 Add pending PKO redirect-link start persistence and consumption, and must follow TDD flow by first writing failing finance persistence/service tests proving start state is tenant/actor scoped, finish resolves the matching pending start, mismatched tenant/actor/state is rejected, consumed or expired state cannot create duplicate connections, and no decrypted secrets are returned before implementing and verifying focused tests.
- [x] 1.3 Wire configured providers so PKO uses the Enable Banking connector and monobank keeps token sync behavior, and must follow TDD flow by first writing failing app/finance wiring tests or focused provider tests proving configured monobank and Enable Banking URLs register the two product-level bank choices and both can run sync through existing job-backed paths before implementing and verifying focused tests.

## 2. Backend Finance API

- [ ] 2.1 Extend the finance OpenAPI contract for PKO redirect start/finish routes, and must follow TDD flow by first writing failing registered-route/controller tests for camelCase request/response shapes, monobank token-only validation, PKO redirect-only validation, callback URLs shaped as `{origin}/#/finance/connections`, localhost/loopback callback allowance, and non-local insecure callback rejection before editing `v1routes.yaml`, regenerating routes, implementing handlers, and verifying focused tests.
- [ ] 2.2 Implement finance controller mapping for token, redirect start, redirect finish, and sync responses, and must follow TDD flow by first writing failing controller tests proving actor/tenant parameters are passed to finance services, pending start metadata is not accepted from the browser on finish, provider errors are sanitized, and successful PKO finish returns a normal `FinanceBankConnection` before implementing and verifying focused tests.

## 3. Finance UI And Client

- [x] 3.1 Extend the finance UI API client for PKO redirect start/finish while keeping existing monobank token linking behavior, and must follow TDD flow by first writing failing client tests proving request paths, camelCase payloads, Date parsing, validation-error propagation, and start/finish support for the `{origin}/#/finance/connections` callback contract before implementing and verifying focused tests.
- [x] 3.2 Replace free-text provider entry on `FinanceConnections.svelte` with static provider-specific monobank and PKO flows, and must follow TDD flow by first writing failing page tests proving no free-text provider input is rendered, no supported-options fetch is required, monobank token submission always uses the monobank option, PKO starts redirect/SCA with callback URL `{origin}/#/finance/connections`, browser return as `{origin}/?code=...&state=...#/finance/connections` finishes the link once and clears the consumed query string while preserving the hash route, and connection/job/schedule visibility remains intact before implementing and verifying focused tests.
- [x] 3.3 Update finance UI behavior documentation for provider-specific linking, and must follow TDD flow by first writing or updating failing documentation-adjacent assertions where available, then updating `apps/signal-ui/ui-wireframe.md` and relevant manual e2e guidance so monobank token linking and PKO via Enable Banking are documented without instructing operators to enter arbitrary providers before verifying the focused UI tests.

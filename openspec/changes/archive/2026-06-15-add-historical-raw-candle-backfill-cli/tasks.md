## 1. Runtime Backfill Runner (`runtime-backfill-runner`)

- [x] 1.1 Add the historical raw candle backfill request/result/report types and validation in the chosen runtime flow package, and follow TDD by writing failing tests for required run ID, venue, symbol, asset class, supported timeframe, half-open time range, and zero-or-positive page size before implementing and verifying focused tests.
- [x] 1.2 Add runner ingestion-run lifecycle orchestration, and follow TDD by writing failing tests proving `started` is recorded before the first venue read with provided run ID, stable source, venue, status `started`, UTC start time, unset completion time, record count `0`, and empty error summary; `succeeded` records persisted count and completion time on success; and `failed` records best-known count plus concise error summary when venue, persistence, or lineage work fails after start before implementing and verifying focused tests.
- [x] 1.3 Add runner candle ingestion through the existing venue ingestion flow with Hyperliquid raw evidence run context, and follow TDD by writing failing mocked-dependency tests proving raw payload IDs from read metadata are linked through the existing ingestion flow, repeated runs keep canonical candles idempotent, new run IDs append raw evidence, and historical trades are not introduced before implementing and verifying focused tests.

## 2. Gap Report And Mocked Hyperliquid Coverage (`gap-report-and-mocked-http`)

- [x] 2.1 Add persisted-candle readback and gap-report computation, and follow TDD by writing failing tests for expected interval counts across supported timeframes, missing interval detection without synthetic candles, first/last persisted boundaries, duplicate natural-key detection when observable, raw payload count inclusion when a cheap run-scoped count is available, deterministic raw payload count omission when it is not cheaply available, and capped missing-interval preview metadata before implementing and verifying focused tests.
- [x] 2.2 Add mocked-HTTP integration-style coverage around the runner and real Hyperliquid adapter, and follow TDD by writing failing tests for multi-page candle backfill raw capture per response, raw-to-candle links, non-2xx/malformed response capture before failure, rerun idempotency with same run ID, and raw evidence append with a new run ID before implementing and verifying focused tests.

## 3. CLI Wiring (`cli-backfill-command`)

- [x] 3.1 Add the `data backfill-raw-candles` Cobra command and app dependency wiring, and follow TDD by writing failing CLI tests proving valid flags map to canonical runner requests, app config/DB/blob/Hyperliquid defaults are reused, invalid inputs return non-zero errors, and no backend route/UI/scheduler path is added before implementing and verifying focused tests.
- [x] 3.2 Add deterministic CLI output rendering, and follow TDD by writing failing CLI tests proving output includes run ID, requested range, persisted count, expected count, gap count, first/last persisted candle boundaries, raw payload count in stable order when present, deterministic raw payload count omission when absent, and capped missing-interval totals in stable order before implementing and verifying focused tests.

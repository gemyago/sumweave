## 1. Account Balance Aggregate Read Path

- [x] 1.1 Add a dedicated finance account-balance aggregate read path and must follow TDD flow by first writing failing finance persistence/service tests proving visible transactions can be aggregated for one account or many accounts, booked and pending balances are separated, hidden transactions are excluded, and regular, refund, transfer, reconciliation, and opening-balance kinds all contribute by signed `amountMinor` before implementing the aggregate query and verifying focused tests.
- [x] 1.2 Wire account-list, account-detail, and standalone single-account balance service reads to include ledger-derived balances and must follow TDD flow by first writing failing finance service tests proving listed and individually loaded accounts include `bookedBalanceMinor` and `pendingBalanceMinor`, and `GetAccountBalance` no longer requires full transaction-history iteration, before implementing and verifying focused tests.
- [x] 1.3 Expose account-list and account-detail balances through the backend HTTP API and must follow TDD flow by first writing failing registered-route controller/OpenAPI tests proving `GET /api/v1/finance/tenants/{tenantId}/accounts` returns camelCase balance fields for each account and `GET /api/v1/finance/tenants/{tenantId}/accounts/{accountId}` returns the selected account with the same balance fields while preserving tenant authorization and existing account fields before updating the OpenAPI contract, regenerating route/model files, implementing mapping, and verifying focused tests.

## 2. Dashboard Balance Read Alignment

- [x] 2.1 Reuse the aggregate account-balance read where dashboard account balances need account-level totals and must follow TDD flow by first writing failing reporting tests proving dashboard account balances match the aggregate balance semantics for the same ledger state and period cutoff before replacing full-history account-balance iteration where it is not needed and verifying focused tests.

## 3. Verification

- [x] 3.1 Complete implementation verification and must follow TDD flow by keeping tests failing before each implementation slice, then running the relevant module test/lint commands plus the repository completion protocol after code changes, and finally executing the manual API-level e2e flow from the design on a migrated local PM2 backend by authenticating as a local test user, creating or reusing a clean tenant, creating 2-3 accounts, recording mixed booked and pending regular, refund, transfer, reconciliation, and opening-balance transactions, calling the tenant account-list endpoint plus account-detail for at least two accounts, verifying expected `bookedBalanceMinor` and `pendingBalanceMinor` values, capturing failing request/response evidence and suspected mismatches for any issue found, implementing targeted fixes, and rerunning the same flow until the balances are correct.

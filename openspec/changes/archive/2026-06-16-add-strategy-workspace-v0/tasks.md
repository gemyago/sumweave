## 1. Strategy Registry And Demo Versions (`strategy-registry-demos`)

- [ ] 1.1 Add the strategy version registry service and persistence in `runtime/strategy`, and follow TDD flow by writing failing tests for create/list/get/version ordering, explicit GORM columns, v0 status semantics (`ready`/`archived`, no persisted `draft`, no status mutation endpoint assumptions), source validation (`human`/`demo`/reserved `ai_draft`), artifact-hash linkage, parent linkage, immutable saved-version behavior, and UTC timestamps before implementing and verifying focused tests.
- [ ] 1.2 Reuse StrategyArtifact validation/canonicalization when saving registry versions, and follow TDD flow by writing failing tests proving valid DSL creates or reuses the canonical artifact, unsupported/code-like fields are rejected, duplicate canonical payloads reuse the artifact hash, and saved registry metadata cannot cause artifact/parameter mismatch before implementing and verifying focused tests.
- [ ] 1.3 Lock the fixed v0 demo definitions and safe default governor policy payload before backend API/UI work, and follow TDD flow by writing failing tests or fixture assertions proving the chosen demo market scopes/windows/ranges are valid canonical Strategy DSL inputs, demo copy states examples/not recommendations and evaluation requires matching local data when not guaranteed, and the default governor policy payload is exactly paper mode with allowed action kinds `long`/`short`, minimum quality `raw`, and maximum approved count `50` before implementing and verifying focused tests.
- [ ] 1.4 Add idempotent demo strategy creation, and follow TDD flow by writing failing tests proving at least three fixed demo versions are created through the same validation/artifact path, are created as `ready` with source `demo`, are not recommendations, can be safely re-seeded without duplicates, and duplicate/edit returns a draftable human candidate response without mutating or persisting a draft row before implementing and verifying focused tests.

## 2. Backend Strategy Workspace API (`backend-strategy-api`)

- [ ] 2.1 Add strategy workspace OpenAPI routes and DTOs in `apps/signal-foundry/internal/api/http/v1routes.yaml`, and follow TDD flow by adding failing controller/contract tests for protected list/get/validate/create-version/duplicate responses with camelCase JSON, including the canonical validation preview payload (`schemaVersion`, `kind`, canonical instrument/timeframe, parameter summary, canonical JSON/hash preview, artifact reuse indicator when knowable, deterministic field errors, and no saved registry row) before regenerating route code and implementing handlers.
- [ ] 2.2 Add the backend strategy workspace application service and route wiring, and follow TDD flow by writing failing tests proving all strategy endpoints require auth, validation returns deterministic errors or canonical preview without saving, saving creates a new immutable `ready` version or reuses an artifact, editing an existing version creates a new version instead of mutating, demos list with source/status labels and no latest-evaluation summary dependency, and duplicate creates a draftable candidate response before implementing and verifying focused tests.

## 3. Backend Evaluation Runner And Evidence API (`backend-evaluation-api`)

- [ ] 3.1 Add the evaluation application service around `flows.DurableBacktestFlow`, and follow TDD flow by writing failing tests proving create requests resolve a `ready` strategy version to artifact, reject mismatched/missing/archived versions, derive instrument/timeframe/kind/parameters only from the artifact, use an explicit policy hash or the fixed default safe governor policy artifact from task 1.3, pass fixed positive quantity, map missing local demo data to a clear replay/data-unavailable failed stage, persist failed stage details, and never execute non-approved governor decisions before implementing and verifying focused tests.
- [ ] 3.2 Add protected evaluation OpenAPI routes/controllers for create/list/detail/report/evidence, and follow TDD flow by writing failing tests proving auth is required, synchronous completed and failed runs return stable status payloads, list filters by strategy and status at minimum, history rows include metrics when available, and detail includes summary, dataset reference, strategy id/version/hash, policy reference, traces, intents, governor decisions, and simulated execution records before regenerating route code and implementing handlers.
- [ ] 3.3 Add compact AI-ready evaluation output metadata without AI calls, and follow TDD flow by writing failing tests proving evaluation detail/report responses include deterministic source/note metadata, omit unavailable metrics rather than emitting misleading zeroes, and contain enough bounded evidence for later critique while no runtime path calls an AI provider before implementing and verifying focused tests.

## 4. Strategy Workspace UI (`ui-strategy-workspace`)

- [ ] 4.1 Add focused strategy/evaluation API client wrappers in `apps/signal-ui`, and follow TDD flow by writing failing tests for auth fetch usage, request serialization, response mapping, validation error mapping, and API error states before implementing and verifying focused tests.
- [ ] 4.2 Add protected strategy routes, nav entries, list, editor, validation panel, canonical preview, and duplicate/save behavior, and follow TDD flow by writing failing component/route tests proving unauthenticated redirects, strategy list columns without latest-evaluation summary dependency, `ready`/`archived` status display, demo labels/example copy, constrained moving-average fields, client-side draft behavior, backend validation preview/error display, immutable saved-version copy, artifact hash display, and run-evaluation action visibility only for evaluatable `ready` versions before implementing and verifying focused tests.
- [ ] 4.3 Add protected evaluation run, history, and detail UI, and follow TDD flow by writing failing component/route tests proving strategy version selection, time-range/quantity validation, synchronous run loading/completed/failed states, history filtering by strategy/status, detail summary/metrics/dataset/artifact/policy rendering, trace/intent/governor/execution tables, and alert/empty states before implementing and verifying focused tests.
- [ ] 4.4 Update `apps/signal-ui/ui-wireframe.md` for the new routes, nav, strategy editor states, evaluation history/detail states, API behavior, accessibility expectations, and no-live-trading/no-AI-runtime constraints, and follow TDD flow by keeping documentation updates with UI behavior changes and verifying focused UI tests cover the documented states.

## 5. Minimal End-To-End Product Flow (`workspace-e2e-flow`)

- [ ] 5.1 Add a happy-path integration/e2e flow only if existing harness support and local candle data are sufficient, and follow TDD flow by writing a failing test or documented skipped/unsupported harness case for login, duplicate demo, edit parameters, validate/save version, run evaluation, open detail, and verify metrics/report/evidence visibility; when local demo data is absent, cover the explicit replay/data-unavailable failed-run semantics instead of fabricating a happy path before implementing and verifying the supported test path.

Chunk ordering: complete sections 1 through 8 strictly in order; do not start a later chunk until focused tests for the current chunk have been written, made to fail, implemented, and passed.

## 1. Generic App Jobs Substrate (`generic-app-jobs-substrate`)

Chunk 1 is review-clean and complete; implementation may proceed to chunk 2.

- [x] 1.1 Refactor the app durable jobs runtime into a generic substrate and must follow TDD flow by first writing failing backend tests for generic JSON input/result/progress storage, namespaced job types, typed handler registration, requester/source/correlation/idempotency metadata, transactional claiming, `queued|running|succeeded|failed|canceled` lifecycle transitions, stale-running recovery, retry-limit behavior, safe cancellation/retry where supported, and preserved historical-backfill behavior before implementing and verifying focused tests.
- [x] 1.2 Add worker-mode and scheduler-tick execution paths and must follow TDD flow by first writing failing command/integration tests for `signal-foundry jobs worker`, `signal-foundry jobs enqueue-due`, database-backed schedule registry state, due-job enqueue idempotency, API-process enqueue-only behavior, and historical plus finance handler dispatch through the same substrate before implementing and verifying focused tests.

## 2. Finance Module Skeleton And Minimal Fixture Scaffolding (`finance-module-foundation`)

- [x] 2.1 Add the root `finance/` module foundation and must follow TDD flow by first writing failing package/service tests proving `finance/` stays independent from `runtime/`, finance domain and persistence models remain separate, finance tables use auto-migrate with `finance_` prefixes and UTC-first timestamps, encrypted credential helpers avoid plain-secret persistence, and finance-owned fixture/scenario seams can be introduced without direct table writes before implementing and verifying focused tests.
- [x] 2.2 Add only minimal finance fixture scaffolding and must follow TDD flow by first writing failing tests for deterministic seed/config plumbing, reusable scenario-builder interfaces/helpers for later tests, and a minimal service-backed bootstrap path that later chunks can extend without yet requiring realistic tenants/connections/transactions or a completed fixture CLI before implementing and verifying focused tests.

## 3. Finance Core Domain Services (`finance-core-domain`)

- [x] 3.1 Add tenant, membership, invite, account, category, and tag services and must follow TDD flow by first writing failing finance-domain tests for tenant creation/list/join flows, equal-member invite acceptance, tenant display currency, tenant-local default-category seeding, account create/update/hide behavior, linked-account attachment to existing manual accounts, tag/category lifecycle, and tenant isolation before implementing and verifying focused tests.
- [x] 3.2 Add transaction ledger behavior and must follow TDD flow by first writing failing finance-domain tests for manual/provider/CSV/system transaction sources, ledger-driven balances, pending vs booked visibility, refund semantics, transfer linking and reporting exclusion, reconciliation/opening-balance behavior, provider-synced hide instead of hard delete, provider-original retention alongside user edits, sorting/filtering needs, and tenant isolation before implementing and verifying focused tests.

## 4. Reporting And FX (`finance-reporting-fx`)

- [x] 4.1 Add dashboard and reporting queries and must follow TDD flow by first writing failing service/query tests for current-month default periods, previous/next month navigation, preset and custom date ranges, settled income/expense/net totals, pending previews, category breakdown tables, account balance summaries, alerts, missing-FX diagnostics, and display-currency versus native-currency reporting behavior before implementing and verifying focused tests.
- [x] 4.2 Add FX providers and finance FX sync jobs and must follow TDD flow by first writing failing backend tests for persisted FX rates by base/quote/date/provider, Frankfurter default-provider parsing, NBP/ECB-ready provider seams, missing-rate surfacing, manual FX sync triggering, scheduled FX sync through generic jobs, and safe admin diagnostics payloads before implementing and verifying focused tests.

## 5. Bank Connections And Provider Sync (`finance-provider-sync`)

- [x] 5.1 Add finance bank-connection services, encrypted credential storage, and generic sync-job contracts and must follow TDD flow by first writing failing finance/backend tests for connection lifecycle state, encrypted-at-rest secrets, re-authentication metadata, provider account normalization, balance snapshot persistence, raw payload retention, idempotent sync keys and pending-to-booked matching, per-connection schedule create/update/pause/resume state, on-demand sync enqueue, scheduled-run visibility metadata, and no-secret logging before implementing and verifying focused tests.
- [x] 5.2 Productize Enable Banking / PKO and monobank connectors and must follow TDD flow by first writing failing fake-provider tests proving redirect/SCA start-finish wiring for Enable Banking, token-based monobank linking, provider range/rate-limit handling, normalized account/transaction ingestion, raw/provider-original retention, and bounded/sanitized provider error behavior without requiring live credentials before implementing and verifying focused tests.

## 6. CSV Imports, Finance HTTP API, And Realistic Fixtures (`finance-imports-api`)

- [ ] 6.1 Add CSV preview/confirm/import services and must follow TDD flow by first writing failing finance/backend tests for tenant-selected import types, header detection, mapping confirmation, preview validation, duplicate detection, account/category/tag would-create previews, durable `finance.csv_import` and `finance.account_import` job execution, rejected-row reporting, and import audit metadata before implementing and verifying focused tests.
- [ ] 6.2 Add finance HTTP/OpenAPI routes and app composition and must follow TDD flow by first writing failing route/controller/integration tests for authenticated `/api/v1/finance/...` tenant create/select/list, tenant member/invite create/list/accept, accounts, connections, transactions, categories, tags, dashboard, FX, and import endpoints, thin delegation into `finance/`, camelCase JSON, generated UI-client compatibility, generic `/api/v1/jobs` list/detail/cancel/retry behavior, finance-job deep links, and sensitive-data redaction before implementing and verifying focused tests.
- [ ] 6.3 Complete realistic finance fixture scenarios plus the CLI entrypoint and must follow TDD flow by first writing failing smoke/integration tests proving `signal-foundry finance fixtures generate ...` calls finance services instead of direct table writes, supports deterministic seed-based output, creates tenants/members/invites/accounts/connections/categories/tags/transactions/FX/job-state scenarios needed for later UI and e2e work, and exposes reusable scenario functions for automated tests before implementing and verifying focused tests.

## 7. Finance UI And Admin Diagnostics (`finance-ui-diagnostics`)

- [x] 7.1 Add the distinct finance UI area and must follow TDD flow by first writing failing UI tests for protected `#/finance` tenant-aware routing, tenant selection/create/invite/join/member flows, dashboard period controls and KPI/alert states, accounts list/detail, transaction filters/sorting/edit/hide/link flows, categories/tags management, connection list/detail states, explicit Enable Banking redirect/SCA and monobank token-linking flows, attach-to-existing-account and re-auth flows, per-connection schedule controls plus last/next sync visibility, CSV import workflows, finance job detail deep links, and separate-detail-route behavior before implementing and verifying focused tests.
- [x] 7.2 Add generic admin jobs/FX/provider diagnostics UI and docs/ops updates and must follow TDD flow by first writing failing UI/doc tests proving `#/admin`, `#/admin/jobs`, `#/admin/jobs/:jobId`, `#/admin/finance/fx`, and `#/admin/finance/providers` surface sanitized operational state, generic job filters/detail metadata, scheduler/sync/FX health cues, links from Data and Finance flows, and matching updates to `docs/ARCHITECTURE.md`, local/backend run instructions, worker/scheduler operational guidance, fixture CLI usage docs, `apps/signal-ui/ui-wireframe.md`, `docs/manual-e2e.md`, and any affected `AGENTS.md` files before implementing and verifying focused tests.

## 8. Dedicated End-to-End Validation And Fix Iteration (`finance-e2e-fix-loop`)

- [x] 8.1 Add dedicated finance end-to-end coverage and must follow TDD flow by first defining failing automated integration/e2e coverage for fixture generation, finance API bootstrapping, worker-mode job execution, scheduler-tick enqueue, tenant invite/member journeys, bank connection link/re-auth/schedule visibility flows, tenant dashboard loading, accounts/transactions workflows, CSV import confirmation and async completion, finance/admin job visibility, FX diagnostics, and finance-to-admin deep links before implementing the coverage and verifying it passes.
- [x] 8.2 Fix issues discovered by the dedicated finance e2e phase and must follow TDD flow by first capturing failing regressions from automated e2e runs or the documented manual browser smoke flow, explicitly including the highest-risk tenant invite/member, bank connection link/re-auth/schedule visibility, and dashboard/accounts/transactions/imports/jobs/FX smoke areas, then implementing targeted fixes and rerunning the same finance e2e/manual smoke loop until the full end-to-end flow is stable and no new blocking regressions remain.

## 9. Post-Review Cleanup Follow-Up (`finance-review-cleanup-follow-up`)

Chunks 1 through 8 remain complete. Execute the follow-up cleanup below strictly in order; do not start a later chunk until focused tests for the current chunk have been written, made to fail, implemented, and passed.

- [ ] 9.1 Regenerate and verify finance/app route artifacts plus config-based wiring and must follow TDD flow by first writing failing backend/UI integration tests proving finance/API route additions come from generated `v1routes` output rather than hand-maintained duplicates, generation leaves no unexpected diff, and finance/app runtime wiring loads settings through config objects instead of reading environment variables directly before implementing and verifying focused tests.
- [ ] 9.2 Remove coverage-ignore escapes and replace ad hoc mocks with project-standard mockery usage and must follow TDD flow by first writing failing focused tests that exercise currently skipped branches through injectable seams, replace manual/mock-by-hand doubles with mockery-generated mocks where appropriate, and verify finance/app coverage no longer depends on `coverage ignore` style exclusions before implementing and verifying focused tests.
- [ ] 9.3 Align finance persistence policy with the user-requested auto-migrate direction and narrow oversized services to cleaner SRP boundaries and must follow TDD flow by first updating planning/policy artifacts to replace the finance explicit-migrations rule with the approved auto-migrate rule, then writing failing backend tests for portable finance schema initialization via auto-migrate plus focused regression tests that lock in slimmer service responsibilities around the affected finance workflows before implementing and verifying focused tests.

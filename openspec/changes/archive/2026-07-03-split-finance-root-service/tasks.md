Chunk ordering: complete sections 1 through 4 strictly in order; sections 1 through 4 are already complete. Execute section 5 only after section 4, and do not start it until focused tests or caller-audit checks for the cleanup have been written, made to fail, implemented, and passed.

## 1. Core Public Service Boundaries

- [x] 1.1 Promote tenant management to a focused public service, and must follow TDD flow by first writing failing finance tests proving tenant create/archive/list/invite/member behavior works through the new tenant service without root `finance.Service`, then exporting the service, narrowing its constructor dependencies, and moving root-service callers/tests for tenant workflows.
- [x] 1.2 Promote catalog management to a focused public service, and must follow TDD flow by first writing failing finance tests proving account/category/tag create/update/hide/list and linked-account attachment work through the new catalog service without root `finance.Service`, then exporting the service, narrowing its store interface, and moving root-service callers/tests for catalog workflows.
- [x] 1.3 Promote ledger transaction workflows to a focused public service, and must follow TDD flow by first writing failing finance tests proving transaction record/update/get/hide/list, transfer linking, and account balance behavior work through the new ledger service without root `finance.Service`, then exporting the service, narrowing its store interface, and moving root-service callers/tests for ledger workflows.

## 2. Reporting, FX, Import, And Bank Sync Services

- [x] 2.1 Promote dashboard reporting to a focused public service, and must follow TDD flow by first writing failing finance tests proving dashboard period resolution, persisted FX use, missing-rate diagnostics, and reporting totals work through the new reporting service without root `finance.Service`, then moving reporting methods and dependencies.
- [x] 2.2 Promote FX synchronization and diagnostics to a focused public service, and must follow TDD flow by first writing failing finance tests proving provider resolution, rate persistence, sync job enqueueing, schedule writing, and admin diagnostics work through the new FX service without root `finance.Service`, then moving FX methods and options.
- [x] 2.3 Promote CSV import preview, confirmation, execution, and audit to a focused public service, and must follow TDD flow by first writing failing finance tests proving import preview/confirm/job/audit behavior works through the new CSV import service without root `finance.Service`, then moving import methods, job enqueuer wiring, and import-specific store access.
- [x] 2.4 Promote bank connection listing, deletion, schedule management, and sync execution to a focused public bank-sync service, and must follow TDD flow by first writing failing finance tests proving list/delete/schedule/trigger/run/apply behavior works through the new bank-sync service while bank-link start/finish/token behavior remains on the existing bank connection service, then moving sync methods, provider dependencies, secret access, and scheduler/enqueuer wiring.

## 3. App, Controller, Job, CLI, And Fixture Wiring

- [x] 3.1 Recompose the `finance.New` / `finance.Finance` public contract and app finance DI around focused services, and must follow TDD flow by first writing failing finance module tests proving `finance.New` returns a `Finance` value that exposes tenant, catalog, ledger, reporting, FX, CSV import, bank-link, and bank-sync services without requiring root `finance.Service`, plus failing app registration tests proving controllers, callback handlers, and jobs resolve only the focused finance services they need, then replacing direct `finance.NewService` app wiring with `finance.New` service fields.
- [x] 3.2 Split finance HTTP controller dependencies by focused service, and must follow TDD flow by first updating controller tests and mocks to prove each endpoint calls the tenant, catalog, ledger, reporting, FX, import, bank-link, or bank-sync dependency matching its workflow while preserving current request and response shapes, then implementing the dependency split for the controller routes, including FX diagnostics and FX sync endpoints.
- [x] 3.3 Split finance job handlers and CLI/fixture bootstrap dependencies by focused service, and must follow TDD flow by first updating job, CLI-relevant, and fixture tests to prove FX sync, CSV import execution, bank sync execution, and scenario seeding use focused services without root `finance.Service`, then implementing the wiring changes.

## 4. Root Service Removal

- [x] 4.1 Remove the root finance service facade and broad service store dependency, and must follow TDD flow by first adding or updating caller-audit tests/checks proving active repo code no longer imports, constructs, or depends on `finance.Service` for product workflows, then deleting or unexporting the root facade, removing obsolete options, and cleaning up tests that only validated facade delegation.

## 5. Delete The Remaining `finance/service.go` Declaration Bucket (`service-go-file-decomposition`)

- [x] 5.1 Delete `finance/service.go`, and must follow TDD flow by first adding or updating focused finance compile-backed tests and caller-audit checks proving the package still exposes the same public params, sentinel errors, tenant seed behavior, and transfer helpers without active code depending on `finance/service.go` as a shared declaration bucket, then moving tenant defaults and tenant params to tenant-focused files, catalog params and related not-found errors to catalog or ledger-focused files, ledger params and transfer helpers to ledger/reporting-focused files, CSV import errors to import-focused files, access-related shared errors to the access guard or another narrowly named concept file if they remain cross-cutting, and finally removing `finance/service.go` without introducing a new broad replacement bucket.

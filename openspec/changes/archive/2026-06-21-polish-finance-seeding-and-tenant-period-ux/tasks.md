Chunk ordering: complete sections 1 through 2 strictly in order; do not start a later chunk until focused tests for the current chunk have been written, made to fail, implemented, and passed.

## 1. Seed Catalog And Yearly Fixture Coverage (`finance-seed-catalog-and-yearly-fixtures`)

- [ ] 1.1 Expand the finance system seed catalog and must follow TDD flow by first writing failing finance-domain tests proving new tenants receive the approved flat default category and default tag baseline, seeded defaults remain tenant-local after copy, and transfer/reconciliation/opening-balance semantics do not rely on seeded user categories before implementing and verifying focused tests.
- [ ] 1.2 Upgrade the realistic finance fixture scenario and CLI path and must follow TDD flow by first writing failing fixture/integration tests proving the default scenario seeds a deterministic full year of activity with roughly 30-40 transactions per calendar month, multiple accounts/currencies, and representative pending/refund/transfer/reconciliation/hidden/import/provider-origin cases before implementing and verifying focused tests.
- [ ] 1.3 Seed reporting FX coverage as part of fixture generation and must follow TDD flow by first writing failing finance/reporting smoke tests proving seeded non-display-currency transactions have the persisted FX data they need immediately after fixture generation, without requiring a follow-up manual sync, before implementing and verifying focused tests.

## 2. Active Tenant And Local Date Workspace UX (`finance-active-tenant-and-local-date-ux`)

- [ ] 2.1 Add one shared active-tenant workspace selection for finance routes and must follow TDD flow by first writing failing UI tests proving a single joined tenant auto-selects as the default workspace choice, multiple joined tenants require one explicit initial choice, direct entry to finance account detail and finance job detail follows the same selection rules without losing the requested route, and the chosen tenant is reused across finance dashboard, accounts, account detail, transactions, categories, connections, imports, and finance job detail until changed before implementing and verifying focused tests.
- [ ] 2.2 Replace raw ISO finance display dates with shared user-local formatting and must follow TDD flow by first writing failing UI/helper tests proving finance pages render user-facing dates and timestamps in a standard local format while preserving existing API/request ISO semantics before implementing and verifying focused tests.
- [ ] 2.3 Synchronize current-month dashboard mode with visible date controls and must follow TDD flow by first writing failing dashboard/UI tests proving initial `current_month` mode, `Current month` reset, and previous/next/custom range navigation all keep the visible date inputs or pickers populated with the active reporting month before implementing and verifying focused tests.
- [ ] 2.4 Update finance UI behavior docs and must refresh `apps/signal-ui/ui-wireframe.md` so the documented finance routes, active-tenant workspace rules, deep-link behavior for account detail and finance job detail, and synchronized current-month controls match the implemented user-visible behavior before closing the chunk.

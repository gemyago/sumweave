## 1. Runtime Availability Reads (`runtime-candle-availability`)

- [ ] 1.1 Add normalized candle availability request/result DTOs and validation in `runtime/data`, and follow TDD flow by writing failing tests for optional exact venue/symbol/asset class filters, default limit 50, maximum limit 200, opaque cursor validation, no timeframe/range/raw-payload filters, supported timeframe metadata, and deterministic validation errors before implementing and verifying focused tests.
- [ ] 1.2 Add grouped normalized candle availability reads from persisted canonical candles, and follow TDD flow by writing failing tests proving raw-only symbols and live-only symbols are excluded, each top-level item is exactly one venue + symbol + asset class entry, each item includes only persisted non-empty timeframe summaries, first/last boundaries and counts are deterministic, top-level entry ordering and cursor pagination are stable, and no candle rows are synthesized before implementing and verifying focused tests.
- [ ] 1.3 Add deterministic per-entry default slices and first-page default selection, and follow TDD flow by writing failing tests proving each item chooses its most-recent timeframe group with stable tie-breakers, the first-page response default mirrors the first returned item default, cursor pages omit top-level default selection, each default range ends at the latest candle end, the range is clipped to the first available start, at most 500 timeframe intervals are requested, and sparse gaps are not filled before implementing and verifying focused tests.

## 2. Backend Availability API (`backend-candle-availability-api`)

- [ ] 2.1 Add the app-owned `GET /api/v1/data/candle-availability` route and schemas, and follow TDD flow by writing failing generated-contract/controller tests for auth protection, query parameters (`venue`, `symbol`, `assetClass`, `limit`, `cursor` only), camelCase response mapping, one item per venue + symbol + asset class, nested timeframe summaries, per-item default slices, first-page default selection fields, cursor-page default omission, empty availability, and read-only behavior before regenerating route code and implementing handlers.
- [ ] 2.2 Wire the availability controller to runtime data reads while preserving candle query requirements, and follow TDD flow by writing failing tests proving the availability endpoint does not call raw payload discovery or live venue symbol APIs, rejects unsupported availability filters, the candle endpoint still rejects missing venue/symbol/asset class/timeframe/start/end, and availability defaults can be passed directly to `/api/v1/data/candles` before implementing and verifying focused tests.

## 3. Browse-First Data UI (`ui-browse-first-data`)

- [ ] 3.1 Update the data API client and authenticated route defaults, and follow TDD flow by writing failing tests for availability query serialization/response mapping with the fixed filters/pagination contract, authenticated empty-root and post-login fallback navigation to `#/data`, preserved explicit `#/chat` access/deep-link return, and protected-route behavior before implementing and verifying focused tests.
- [ ] 3.2 Replace the blank-first data page with availability-first browsing, and follow TDD flow by writing failing tests proving `#/data` loads the first availability page on open, renders available venue + symbol + asset class entries with nested timeframe/range/count summaries, selects the response default entry, calls the candle endpoint with the exact default slice only, does not auto-call the broad raw payload metadata list, renders returned candles, and selects a default candle when returned data exists before implementing and verifying focused tests.
- [ ] 3.3 Keep explicit filter/range editing and evidence behavior around the selected normalized candle scope, and follow TDD flow by writing failing tests proving selecting any availability entry uses that item’s per-entry default slice, manual timeframe/range edits still use the existing candle validation, no-availability state does not guess candle filters, API errors use alert semantics, default-selected and manually selected candles load linked raw evidence by provenance, and broad raw payload browsing remains explicit and secondary to the selected normalized candle scope before implementing and verifying focused tests.
- [ ] 3.4 Update the data browser wireframe documentation, and follow TDD flow by keeping documentation changes with the UI behavior task and verifying focused UI tests still cover the documented browse-first states.

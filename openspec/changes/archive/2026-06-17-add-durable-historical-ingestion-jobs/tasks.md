## 1. Backend Jobs Foundation (`jobs-foundation`)

- [x] 1.1 Add durable app-owned job models, store, migrations, and lifecycle transitions, and must follow TDD flow by first writing failing store/service tests for explicit GORM columns, queued/running/succeeded/failed transitions, requester/source metadata, timestamps, worker/attempt metadata, bounded error fields, list/get pagination filters, idempotency-key same-input reuse, idempotency-key different-input conflict without mutation, and restart-visible persisted rows before implementing and verifying focused tests.
- [x] 1.2 Add historical raw candle backfill job validation and creation, and must follow TDD flow by first writing failing service tests proving canonical input validation for Hyperliquid perps futures, symbol/timeframe/range/page-size limits, no future end, configured max intervals, generated job id plus ingestion run id, immediate queued response, narrow idempotency reuse only for the same canonical input hash, 409/conflict semantics for key/hash mismatch, and no instrument/range over-deduplication before implementing and verifying focused tests.
- [x] 1.3 Add the historical backfill worker/executor and lifecycle wiring, and must follow TDD flow by first writing failing worker tests for idempotent claim, attempt-count increment on claim, queued-to-running-to-succeeded, failed state with bounded safe errors, duplicate message/poll skipping terminal jobs, startup requeue of stale running jobs below the attempt cap with specified metadata changes, startup failure of stale running jobs at/above the attempt cap, no age-only reclaim of current in-process jobs, config-enabled startup, max concurrent backfill limit of one by default, and execution through the existing runtime backfill runner before implementing and verifying focused tests.

## 2. Jobs HTTP API (`jobs-http-api`)

- [x] 2.1 Add protected app OpenAPI routes and generated controller glue for `/api/v1/jobs`, and must follow TDD flow by first writing failing route/controller tests proving camelCase create/list/detail DTOs, authentication enforcement, requester source `operator`, status/jobType/source/limit/cursor filters, safe validation/not-found/error mappings, and immediate queued create response before editing `internal/api/http/v1routes.yaml`, generating code, implementing controllers, and verifying focused tests.
- [x] 2.2 Add backend integration coverage for API-created historical backfill jobs, and must follow TDD flow by first writing a failing integration-style test using a mocked/test venue path proving API create persists a queued job, worker executes the existing backfill runner, terminal detail contains ingestion run id/counts/gaps/raw payload metadata, persisted candles are readable through the existing data read path, and process-restart-visible state comes from the jobs table before implementing and verifying the test.

## 3. Agent Job Tools And Skills (`jobs-agent-tools-skills`)

- [x] 3.1 Add strategy assistant job tool contracts and registration, and must follow TDD flow by first writing failing strategy assistant tests proving `sf_jobs_start_historical_data_backfill`, `sf_jobs_list`, and `sf_jobs_get` are registered with internal-alpha bounded descriptions, call the job service directly, use stable safe error/truncation conventions, include agent requester metadata when available, return job id/status/detail, instruct polling before evaluation, and expose no live trading/raw SQL/shell behavior before implementing and verifying focused tests.
- [x] 3.2 Add historical data job workflow skill guidance, and must follow TDD flow by first writing failing skills/smoke fixture checks proving the bundled skill is discoverable and documents availability check, duplicate queued/running job avoidance, bounded backfill start, polling to terminal, re-checking data, synchronous evaluation only after success, failed-job summarization, no invented data, no continuous ingestion, and no real-money execution before adding/updating skill files and verifying focused tests.

## 4. Operator UI Jobs Workspace (`jobs-ui-workspace`)

- [x] 4.1 Add UI Jobs API client, nav, protected routes, list page, and detail page, and must follow TDD flow by first writing failing UI/client tests for authenticated fetch behavior, empty/loading/error/success list states, status/jobType/source filters, refresh/open actions, separate detail route, queued/running/succeeded/failed detail rendering, input/requester/timeline/worker/result/missing-preview/error fields, and Data-page backlink before implementing and verifying focused tests.
- [x] 4.2 Add Data-page historical backfill start action and wireframe updates, and must follow TDD flow by first writing failing UI tests proving the action is explicit, reuses the selected/current data scope, validates required fields without implicit ingestion, posts to the job create endpoint, shows created job status/link, preserves read-only browse/load/select behavior by default, and updates `ui-wireframe.md` to document the new behavior before implementing and verifying focused tests.

## 5. Integration And Documentation (`jobs-integration-docs`)

- [x] 5.1 Add executable end-to-end product-flow coverage and docs/status updates, and must follow TDD flow by first defining a failing automated local flow covering operator/API start, worker terminal success, job list/detail visibility, Data page candle visibility after success, existing synchronous evaluation unchanged, agent tool start/list/get path, and skill discovery before implementing the coverage and updating relevant docs/status artifacts.

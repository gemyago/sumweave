Chunk ordering: complete sections 1 through 4 strictly in order; do not start a later chunk until focused tests for the current chunk have been written, made to fail, implemented, and passed.

## 1. App-Owned Pub/Sub Dispatch Foundation (`app-pubsub-foundation`)

- [ ] 1.1 Define the internal execution-dispatch boundary and must follow TDD flow by first writing failing backend tests proving `apps/signal-foundry/` exposes one app-owned execution publisher/handler seam with a versioned execution envelope, one durable execution-dispatch topic, typed handler registration by dispatch kind, and no Watermill imports or Watermill-shaped message types crossing into `finance/` or `runtime/`, before implementing and verifying focused tests.
- [ ] 1.2 Add the SQL-backed Watermill transport adapter and migration wiring and must follow TDD flow by first writing failing backend/migration tests proving the transport uses the app database in the repo's SQLite-first local shape, remains compatible with SQL deployment shapes, is prepared only by `signal-foundry db-migrate`, is not auto-created by startup commands, and can durably persist a publish in the same app-owned transaction as caller-requested database changes, before implementing and verifying focused tests.

## 2. Durable Jobs Watermill Dispatch (`durable-jobs-watermill-dispatch`)

- [ ] 2.1 Convert on-demand execution paths to durable publish-first acceptance and must follow TDD flow by first writing failing service/integration tests proving request validation finishes before durable writes, accepted responses are returned only after durable publish succeeds, observable workflows can create or update their initial jobs metadata in the same commit as publish, publish failure leaves no accepted observation state behind, and API/request paths never execute background work inline, before implementing and verifying focused tests.
- [ ] 2.2 Convert jobs execution mode from polling-first behavior to consumer-based dispatch and must follow TDD flow by first writing failing backend/command tests proving the consumer routes by dispatch kind from the decoded envelope, executes the intended workflow handler from canonical payload, updates observable status/result/progress metadata when those records exist, applies duplicate-delivery guards only when a workflow explicitly declares them, and does not blindly replay successfully completed business work solely because a non-essential observation write failed, before implementing and verifying focused tests.

## 3. Tick-Driven Scheduling On The Same Dispatch Path (`scheduler-dispatch-unification`)

- [ ] 3.1 Update scheduler behavior so ticks publish scheduled-work dispatch messages and must follow TDD flow by first writing failing scheduler/integration tests proving due-schedule scanning, due-window advancement and scheduled-work publish commit together atomically, failed publish leaves the due window eligible for a later tick, observable scheduled workflows update visible job history when they expose it, and scheduler ticks never scan, execute, retry, or recover immediately initiated jobs, before implementing and verifying focused tests.
- [ ] 3.2 Preserve dedicated command modes while aligning them to the new dispatch flow and must follow TDD flow by first writing failing command tests proving `signal-foundry start` stays API-only, `signal-foundry jobs worker` runs only the dedicated durable consumer mode, and `signal-foundry jobs enqueue-due` performs exactly one scheduler tick and returns tick errors without executing durable work inline, before implementing and verifying focused tests.

## 4. Local `start-all` Mode And Ops Guidance (`start-all-local-shape`)

- [x] 4.1 Add `signal-foundry start-all` as the standard local backend mode and must follow TDD flow by first writing failing command/startup tests proving `start-all` composes the same HTTP server, consumer, and scheduler-tick components used by dedicated commands, runs one scheduler tick immediately after startup and then on a non-overlapping fixed-delay loop from explicit scheduler-loop config, cancels sibling components and exits non-zero on startup failure or unexpected server/consumer termination, continues later ticks after a tick error without spawning overlapping loops, and never runs app-owned schema migration implicitly during startup, before implementing and verifying focused tests.
- [x] 4.2 Update architecture and runbook guidance and must follow TDD flow by first defining failing doc-smoke checks or explicit review checkpoints proving backend docs, AGENTS instructions, PM2/local run guidance, and database migration guidance all describe `signal-foundry db-migrate` plus `signal-foundry start-all` as the normal local backend workflow while still documenting `start`, `jobs worker`, and `jobs enqueue-due` for split or production-like environments, before implementing and verifying the required updates.

## 1. Audit and intent dependency foundation

- [x] 1.1 Add `DecisionTrace` and `OrderIntent` domain validation, service behavior, persistence, and query helpers; must follow TDD flow (write test -> implement -> verify) by first adding unit and SQLite-backed tests for required fields, UTC normalization, every intent referencing exactly one trace, approved/rejected/blocked/error trace paths, status transitions, metadata bounds, idempotent writes, and query by strategy, instrument, mode, and time range.
- [x] 1.2 Link the thin flow to trace and intent records before governor evaluation; must follow TDD flow (write test -> implement -> verify) by first adding a flow integration test that proves trace creation precedes intent creation and stable intent context is prepared for the next governor boundary without asserting governor decisions, execution ledger records, or downstream reference write-back, and without adding UI, backend API, live venue, or AI dependencies.

## 2. Governor evaluator expansion

- [x] 2.1 Expand governor policy/input/domain reason codes for paper/backtest intent evaluation; must follow TDD flow (write test -> implement -> verify) by first adding validation tests for explicit mode, venue, instrument, strategy/artifact reference, candidate/action kind, quantity/notional, limit price when present, quality, exposure inputs, governor policy id/version/hash, invalid live/unsupported intent inputs, and canonical uppercase snake-case persisted reason-code strings.
- [x] 2.2 Implement deterministic mode, scope, quality, kill-switch, notional, exposure, and approval-count checks; must follow TDD flow (write test -> implement -> verify) by first adding unit tests for canonical persisted reason-code values `OK`, `MODE_NOT_ALLOWED`, `VENUE_NOT_ALLOWED`, `INSTRUMENT_NOT_ALLOWED`, `STRATEGY_NOT_ALLOWED`, `ACTION_KIND_NOT_ALLOWED`, `DATA_QUALITY_TOO_LOW`, `KILL_SWITCH_ACTIVE`, `ORDER_NOTIONAL_EXCEEDS_LIMIT`, `STRATEGY_EXPOSURE_EXCEEDS_LIMIT`, `INSTRUMENT_EXPOSURE_EXCEEDS_LIMIT`, `APPROVAL_LIMIT_REACHED`, `INVALID_INTENT`, and repeated-evaluation determinism.

## 3. Paper execution ledger and deterministic limit-fill simulator

- [x] 3.1 Add execution command/order/fill ledger persistence with idempotent command-first writes; must follow TDD flow (write test -> implement -> verify) by first adding runtime execution and SQLite tests for command, order, and fill schema migration, explicit columns, UTC timestamps, trace/intent/governor references when available, deterministic client order id behavior, retry idempotency, and unsupported live mode rejection.
- [x] 3.2 Add the deterministic limit-fill simulator and ledger integration; must follow TDD flow (write test -> implement -> verify) by first adding tests for limit-only validation, buy/long fill when a later closed candle reaches the limit, sell/short fill when a later closed candle reaches the limit, no-fill paths, full-fill-only behavior, deterministic zero fee/slippage metadata, no duplicate fills on retry, and no live network dependency.

## 4. Paper position and portfolio snapshots

- [x] 4.1 Add deterministic position projection from fills plus persistence/query behavior; must follow TDD flow (write test -> implement -> verify) by first adding unit and SQLite tests for event-time/fill-id ordering, open long, increase long, reduce long, flatten long, open short, reduce short, unsupported reversal handling, exposure notional, source fill linkage, UTC timestamps, and query by strategy, instrument, mode, and time.
- [x] 4.2 Add portfolio snapshot projection plus persistence/query behavior; must follow TDD flow (write test -> implement -> verify) by first adding unit and SQLite tests for gross/net exposure, realized PnL aggregation, optional unrealized PnL only when a deterministic mark/reference price is supplied, metadata documenting perps assumptions, and query by mode and time.

## 5. Backtest run and evaluation report scaffold

- [x] 5.1 Add compact dataset reference records and `BacktestRun` lifecycle persistence; must follow TDD flow (write test -> implement -> verify) by first adding runtime backtest and SQLite tests for dataset reference identity fields, source hashes or replay checksum, pending/running/completed/failed transitions, immutable strategy/governor/dataset references, compact versioned metrics JSON, failure details, UTC timestamps, and query behavior.
- [x] 5.2 Add `EvaluationReport` persistence and minimal metrics assembly; must follow TDD flow (write test -> implement -> verify) by first adding tests that reports reference backtest, strategy artifact, and dataset records, store decisions from the documented subset, include only derivable compact metrics, preserve stable failure reasons, omit unsupported metrics instead of writing misleading zeros, and can be queried deterministically.
- [x] 5.3 Connect the flow-level integration path across audit, governor, execution ledger, snapshots, backtest run, and evaluation report records; must follow TDD flow (write test -> implement -> verify) by first adding an end-to-end in-memory/SQLite integration test that verifies deterministic ordering and linkage from `DecisionTrace` to `OrderIntent` to governor decision to execution command/order/fill to snapshot and report without adding backend routes, UI screens, live trading, or AI calls.

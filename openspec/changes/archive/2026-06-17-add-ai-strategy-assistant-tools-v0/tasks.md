## 1. Tool Contracts And Registration (`strategy-assistant-tool-contracts`)

- [ ] 1.1 Add the app-owned strategy assistant tool registration component and explicit DTO contracts, and must follow TDD flow by first writing failing tests proving all expected tool names are registered in `agent.ToolsRegistry`, descriptions document bounded/internal-alpha behavior, request/response structs use camelCase JSON-compatible fields, duplicate registration is not required, and no live trading/manual order/raw SQL tool name is present before implementing and verifying focused tests.
- [ ] 1.2 Define shared tool error/result conventions for recoverable validation, not found, conflict, data-unavailable, not-ready, unsaved-version, missing-artifact, and truncation states, and must follow TDD flow by first writing failing handler tests proving deterministic service errors use stable machine-actionable `error.code`/`error.message`/field-error fields, bounded results use deterministic truncation metadata and next-step hints, and no SQL/table/GORM internals leak before implementing and verifying focused tests.

## 2. Data Browsing Tools (`strategy-assistant-data-tools`)

- [ ] 2.1 Implement `sf_data_list_candle_availability`, and must follow TDD flow by first writing failing tests with lightweight fakes for exact optional venue/symbol/assetClass filters, pagination/cap handling, compact timeframe/count/start/end/default-selection output, empty availability behavior, and no mutation/backfill side effects before implementing and verifying focused tests.
- [ ] 2.2 Implement `sf_data_get_candles`, and must follow TDD flow by first writing failing tests proving exact scope/range inputs are required, unsupported values and invalid UTC ranges are rejected, output is deterministic ascending normalized candle data with stable provenance references, response size is capped/truncated honestly, and missing candles are returned as empty rather than synthesized before implementing and verifying focused tests.
- [ ] 2.3 Implement `sf_data_get_candle_evidence`, and must follow TDD flow by first writing failing tests proving the tool mirrors the linked-evidence contract, requires selected-candle provenance source/identity, returns bounded raw payload metadata only, represents absent links as an empty collection, and never returns raw response body bytes or raw SQL access before implementing and verifying focused tests.

## 3. Strategy Version Tools (`strategy-assistant-strategy-tools`)

- [ ] 3.1 Implement `sf_strategy_list_versions` and `sf_strategy_get_version`, and must follow TDD flow by first writing failing tests proving compact metadata listing, deterministic version/detail mapping, saved definition inclusion only on get, not-found behavior, source/status labels, artifact hash/schema/kind/instrument/timeframe fields, and no evaluation aggregation dependency before implementing and verifying focused tests.
- [ ] 3.2 Implement `sf_strategy_validate_definition`, and must follow TDD flow by first writing failing tests proving strict Strategy DSL v0 validation/canonicalization is delegated to the workspace service, successful responses include canonical preview/hash/existing-artifact data, failed responses include deterministic field errors, and validation never persists a version before implementing and verifying focused tests.
- [ ] 3.3 Implement `sf_strategy_duplicate_version` and `sf_strategy_create_version`, and must follow TDD flow by first writing failing tests proving duplicate returns an editable candidate without mutating the source version, create saves only valid immutable versions through the workspace save path, parent linkage/notes/display metadata are preserved, conflicts/not-found/errors are deterministic, and no arbitrary executable code or validation bypass is accepted before implementing and verifying focused tests.

## 4. Evaluation Backtest Tools (`strategy-assistant-evaluation-tools`)

- [ ] 4.1 Implement `sf_evaluation_run_backtest`, and must follow TDD flow by first writing failing tests proving inputs are limited to strategy id/version, UTC range, quantity, optional governor policy hash, and note; the service derives runtime inputs from the saved ready version's immutable artifact; draft/edit candidates, unsaved definitions, saved non-ready versions, and saved versions with missing/unloadable artifacts are rejected with stable structured errors and no run creation; omitted policy uses existing default behavior; missing local data returns/persists a clear failed result; and non-approved governor decisions are not executed before implementing and verifying focused tests.
- [ ] 4.2 Implement `sf_evaluation_list_backtests`, `sf_evaluation_get_backtest_detail`, and `sf_evaluation_get_backtest_report`, and must follow TDD flow by first writing failing tests proving strategy/status filters, stable compact history rows, detail/report mapping, omitted unavailable metrics, dataset/policy/artifact references, failed-run reason/details, and bounded AI-ready summaries before implementing and verifying focused tests.
- [ ] 4.3 Implement `sf_evaluation_get_backtest_evidence`, and must follow TDD flow by first writing failing tests proving bounded persisted evidence rows for traces, order intents, governor decisions, execution records, and snapshots are returned with counts/truncation metadata, missing optional evidence is explicit, and no fabricated metrics/evidence or raw database access is exposed before implementing and verifying focused tests.

## 5. Runtime Wiring, Profile, Skills, And UI Polish (`strategy-assistant-alpha-flow`)

- [ ] 5.1 Wire the strategy assistant tool pack into `apps/signal-foundry` runtime construction, and must follow TDD flow by first writing failing app wiring tests proving data, strategy, and evaluation services are injected, tools are registered before `agent.NewRunner` creates tool stubs, existing workspacefs/skills tools still register, and runtime startup has no service dependency cycle before implementing and verifying focused tests.
- [ ] 5.2 Add `strategy-assistant` profile guidance or idempotent seed, and must follow TDD flow by first writing failing tests or fixture checks proving the profile/payload uses regular execution settings, instructs data discovery → validate/save → evaluate → evidence critique workflow, warns against live trading/readiness claims/bypasses, and can be created or discovered without a new workflow UI before implementing and verifying focused tests.
- [ ] 5.3 Add bundled strategy workflow skills and make them discoverable in the alpha smoke path, and must follow TDD flow by first writing failing skills/runtime tests or fixture checks proving `skills_list` exposes `strategy-research-loop`, `backtest-critique`, and `strategy-iteration`, `skills_read` returns concise operational instructions with safety boundaries, and the app configuration/profile path enables their use without mandatory manual setup before implementing and verifying focused tests.
- [ ] 5.4 Add minimal chat UI/profile polish needed for the automated alpha flow, and must follow TDD flow by first writing failing UI tests or executable route/component checks proving the operator can use/select the strategy assistant profile, observe tool calls/results, and follow strategy/evaluation identifiers or links while no new complex workflow UI or live-trading control is added before implementing and verifying focused tests.

## 6. Integration Smoke Coverage (`strategy-assistant-smoke`)

- [ ] 6.1 Add executable automated integration-style smoke coverage for the alpha loop, and must follow TDD flow by first defining the failing/expected local-repo flow for list data availability, read bounded candles, validate a strategy, save a ready version, run an evaluation, read report/evidence, and load a strategy workflow skill; the supported path must run by default without manual UI clicks, and if local data is absent it must assert the explicit data-unavailable failed-run behavior instead of fabricating a successful backtest before implementing and verifying the supported path.
- [ ] 6.2 Add a clearly labeled conditional manual fallback runbook only for any acceptance coverage that cannot be automated in this slice, and only if such a gap remains after 6.1; the deliverable must identify the automation gap, exact prerequisite configuration, manual steps, expected tool/error/truncation observations, and why the automated smoke path remains the primary default gate.

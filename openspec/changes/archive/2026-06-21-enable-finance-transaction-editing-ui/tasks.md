## 1. Finance Transaction Detail And Edit Contract

- [x] 1.1 Extend finance transaction read/update behavior in `finance/` and must follow TDD flow by first writing failing finance-domain tests proving tenant-scoped transaction detail reads return the selected record, tenant-scoped edits update `description`, `amountMinor`, `effectiveAt`, and `categoryId`, allow category clearing, and preserve provider-original plus non-editable transaction fields before implementing and verifying focused tests.
- [x] 1.2 Add the finance transaction detail and patch endpoints plus OpenAPI contract in `apps/signal-foundry/` and must follow TDD flow by first writing failing controller/route tests proving authenticated tenant members can `GET` and `PATCH` `/api/v1/finance/tenants/{tenantId}/transactions/{transactionId}` with camelCase payloads, receive `providerOriginal` when present, and hit tenant/category authorization guards before implementing, regenerating any affected API surface, and verifying focused tests.
- [x] 1.3 Update finance UI API helpers for transaction list/detail/create/edit calls and must follow TDD flow by first writing failing `apps/signal-ui/src/lib/finance` tests proving transaction detail responses parse provider-original data correctly, create requests remain supported through the shared editor flow, and bounded edit requests submit the expected nullable-category payload before implementing and verifying focused tests.

## 2. Finance Transaction Create And Edit UI

- [x] 2.1 Add the shared `#/finance/transactions/new` and `#/finance/transactions/:transactionId` routes and refresh `apps/signal-ui/ui-wireframe.md`, and must follow TDD flow by first writing failing route/page tests proving direct entry and in-app navigation both resolve the active tenant workspace, keep finance navigation context intact, initialize create mode correctly, and load the selected transaction through the dedicated detail endpoint for edit mode before implementing and verifying focused tests.
- [x] 2.2 Add the shared finance transaction editor form and must follow TDD flow by first writing failing UI tests proving create and edit modes reuse the same screen, operators can edit `description`, `amountMinor`, `effectiveAt`, and `categoryId`, provider-origin comparison values appear for synced transactions, and save or error feedback works on both modes before implementing and verifying focused tests.
- [x] 2.3 Make the shared transaction editor mobile friendly and must follow TDD flow by first writing failing UI tests proving the screen keeps a usable single-record action hierarchy on narrow viewports, preserves save/cancel access, and keeps secondary provider/state context readable without depending on a wide two-column layout before implementing and verifying focused tests.
- [x] 2.4 Refocus the transactions list on browsing and navigation and must follow TDD flow by first writing failing UI tests proving the inline create form is removed, a clear create-transaction entry action opens the shared editor, transaction list items expose an obvious open/edit action, and pending/hidden/transfer/refund/reconciliation state cues stay visible around those entrypoints before implementing and verifying focused tests.

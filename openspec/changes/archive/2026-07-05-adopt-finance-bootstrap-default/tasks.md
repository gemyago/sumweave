Implementation MUST preserve strict parent-task ordering: complete parent task 1, then 2, then 3, then 4, then 5, then the final-review correction task 6. This change supersedes `restructure-finance-ui-shell` for Finance/login styling; implementers may reuse only behavior-level shell and tenant lessons from that change, such as route coverage, route-preserving tenant selection, supported navigation mapping, and avoiding dead routes. Do not carry forward its custom-shell or terminal-native styling direction as the canonical Finance/login acceptance target, and do not preserve legacy `#/v2/*` finance/login compatibility routes without explicit user approval.

## 1. Canonical Login And Routing

- [x] 1.1 Promote Bootstrap login to canonical `#/login`, and must follow TDD flow by first adding failing route/login tests proving `#/login` renders the Bootstrap form, preserves loading/error/disabled behavior, honors remembered protected destinations, and no longer exposes pilot product naming before implementing the canonical login composition and visual verification flow for desktop/mobile login.
- [x] 1.2 Make Finance the default authenticated destination, and must follow TDD flow by first adding failing routing tests proving successful sign-in without a remembered destination and authenticated root navigation land on `#/finance`, while remembered protected destinations still win, before implementing the routing constant and redirect changes.

## 2. Bootstrap Finance Shell Foundation

- [x] 2.1 Wire one Bootstrap Finance shell around all `#/finance*` routes, and must follow TDD flow by first adding failing route/component tests proving dashboard, tenants, accounts, account detail, connections, synthetic setup, transactions, transaction create, transaction edit, categories, imports, and finance job detail render inside shared Bootstrap finance chrome with no legacy finance subnav or `restructure-finance-ui-shell` custom styling as primary navigation before implementing shell routing, behavior-only reuse from prior shell/tenant work, and the shell visual verification flow.
- [x] 2.2 Preserve tenant context in the Bootstrap shell, and must follow TDD flow by first adding failing shell-state tests proving sole-tenant auto-selection, multi-tenant route-preserving selection, one shared shell tenant control, and deep-link continuity for detail/editor/job/synthetic routes before implementing the shell tenant-control changes.

## 3. Canonical Finance Dashboard And Navigation

- [x] 3.1 Promote the Bootstrap dashboard to canonical `#/finance`, and must follow TDD flow by first adding failing dashboard tests proving the route uses existing finance dashboard/account/transaction/category/connection data, shows balance-first summaries, reporting-period controls, capped account/category/recent-transaction sections, attention states including missing-FX signals, and honest loading/empty/error states before implementing the dashboard and its visual verification flow.
- [x] 3.2 Align Finance navigation and route documentation for canonical Bootstrap routes, and must follow TDD flow by first adding failing shell/navigation tests proving all supported Finance destinations use canonical `#/finance*` links without pilot product naming before implementing navigation changes; update only the route/navigation portions of `apps/signal-ui/ui-wireframe.md` in the same slice so the documented Finance links, tenant-shell entry points, and absence of pilot naming match the tested behavior.

## 4. Remaining Finance Route Surfaces

- [x] 4.1 Convert tenant, account, account-detail, and category routes to Bootstrap, and must follow TDD flow by first adding failing page tests for Bootstrap-first headings, forms, cards/lists/tables, actions, tenant-required states, loading/empty/error states, and route links before implementing the route group and its visual verification flow.
- [x] 4.2 Convert connection, synthetic setup, import, and finance job detail routes to Bootstrap, and must follow TDD flow by first adding failing page tests for Bootstrap-first provider choices, step/preview/confirmation states, async job links/status, setup validation/retry states, and no raw secret/provider payload disclosure before implementing the route group and its visual verification flow.
- [x] 4.3 Convert transactions browse and transaction editor routes to Bootstrap, and must follow TDD flow by first adding failing page/route tests proving browse filters, summary chips, ledger table, responsive selected-transaction context, dedicated create/edit forms, provider-original value visibility, save/cancel behavior, and loading/empty/error states before implementing the route group and its visual verification flow.

## 5. Rules And Manual E2E Documentation

- [x] 5.1 Perform documentation acceptance for Bootstrap rules and manual smoke coverage after the implementation slices, not as a TDD task: verify the relevant docs describe canonical login, default Finance landing, Finance shell, dashboard, route-group, responsive, non-finance-regression smoke paths, and the `restructure-finance-ui-shell` supersession decision, then update `apps/signal-ui/AGENTS.md`, `apps/signal-ui/DESIGN.md`, manual e2e guide/index entries, and any remaining non-route `ui-wireframe.md` acceptance text so future agents treat Bootstrap Finance as canonical.

## 6. Final Review Corrections

- [x] 6.1 Retire the remaining legacy `#/v2/*` finance/login surface, and must follow TDD flow by first adding or updating failing route/routing tests proving `#/v2/login` and `#/v2/finance` are no longer registered supported routes, no protected-destination logic preserves `#/v2/finance`, and canonical login/Finance behavior still passes, before removing the legacy route constants, route registrations, V2 shell rendering, compatibility tests, and compatibility-only references from `apps/signal-ui/AGENTS.md`, `apps/signal-ui/ui-wireframe.md`, and the manual smoke guide.
- [x] 6.2 Align responsive Finance shell documentation with the implemented Bootstrap shell, as a documentation/manual-acceptance correction: update `apps/signal-ui/ui-wireframe.md` and `docs/manual-e2e/finance-ui-shell-smoke-e2e.md` so narrow viewport behavior describes the current stacked full-width Bootstrap aside/nav plus compact utility/header controls rather than an explicit menu toggle, then rerun the responsive smoke path and record evidence in the chunk review. Do not add a new responsive toggle unless the current shell fails the smoke acceptance.

Chunk ordering: complete sections 1 through 5 strictly in order; do not start a later chunk until focused tests for the current chunk have been written, made to fail, implemented, and passed. Section 6 is the whole-change completion check and must run only after the Enable Banking HTTPS callback correction in section 5 is complete.

## 1. Command Skeleton And Shared POC Utilities (`finance-poc-command-foundation`)

- [ ] 1.1 Add the `finance-poc` Cobra command skeleton, shared JSON/file/date/error helpers, and security guardrails, and must follow TDD flow by first writing failing command tests for root wiring under `signal-foundry finance-poc`, provider subcommand discovery, `--json` stdout behavior, stderr progress separation, env/flag config precedence, context timeout handling, owner-only output-file creation where practical, bounded provider error excerpts, and no secret/token/JWT/private-key leakage before implementing and verifying focused tests.

## 2. Enable Banking / PKO POC (`enable-banking-pko-poc`)

- [ ] 2.1 Add Enable Banking authentication and ASPSP discovery, and must follow TDD flow by first writing failing tests with generated RSA keys and `httptest.Server` proving RS256 JWT signing, JWT header `kid` from app id, expected `iss`/`aud`/`iat`/`exp` claims, private key loading from `ENABLE_BANKING_PRIVATE_KEY_PATH`, `Authorization: Bearer` request header without logging the token, `GET /aspsps?country=PL`, `--base-url`/env override support, raw JSON preservation, and `enable-banking aspsps --country PL --json` command output before implementing and verifying focused tests.
- [ ] 2.2 Add Enable Banking authorization/session commands, and must follow TDD flow by first writing failing tests proving `start-auth` generates random state, calls `POST /auth`, writes the documented pending auth file to `--auth-file` before printing the authorization URL, does not auto-open by default, supports explicit `--open-browser` only as an opt-in seam, and proving manual `finish-session --auth-file ... --code ... --state ... --session-file ... --json` loads persisted pending auth data, verifies state, exchanges code through `POST /sessions`, and writes the documented session-file shape against a fake server; tests must also prove `connect` provides a local-callback convenience flow for `--callback-listen-addr`, rejects callback state mismatch, and uses the same exchange/session-file behavior before implementing and verifying focused tests.
- [ ] 2.3 Add Enable Banking accounts and transactions commands, and must follow TDD flow by first writing failing tests proving session-file loading, `GET /sessions/{session_id}`, optional `GET /accounts/{uid}/details`, optional `GET /accounts/{uid}/balances`, `GET /accounts/{account_id}/transactions` with `date_from`/`date_to`/optional `strategy`/straightforward status mapping, continuation-key pagination until exhausted, raw payload preservation, thin normalized account/transaction summaries, optional `--out` export writing, and stderr summary counts with JSON stdout before implementing and verifying focused tests.

## 3. monobank Personal API POC (`monobank-personal-poc`)

- [ ] 3.1 Add monobank account listing, and must follow TDD flow by first writing failing fake-server command/client tests proving token loading from `MONOBANK_TOKEN` or `--token`, `MONOBANK_BASE_URL`/`--base-url` override support, `X-Token` header transmission without logging the token, `GET /personal/client-info`, normalized accounts/jars/managed-client accounts with account id/type/currency code/balance/credit limit/masked PAN/IBAN when present, raw payload preservation, and `monobank accounts --json` output before implementing and verifying focused tests.
- [ ] 3.2 Add monobank transaction fetching, and must follow TDD flow by first writing failing tests proving `--account` default `0`, `--from`/`--to` `YYYY-MM-DD` parsing to Unix seconds, request splitting into chunks of at most 31 days plus 1 hour, safe default `61s` inter-chunk sleep with test override to no/short sleep, `GET /personal/statement/{account}/{from}/{to}` endpoint paths, raw statement preservation, thin normalized transaction summaries, optional `--out` export writing, and stderr summary counts with JSON stdout before implementing and verifying focused tests.

## 4. Documentation And Ignore Rules (`financial-poc-docs-ignore-rules`)

- [ ] 4.1 Add required financial POC documentation and ignore-rule safeguards, and must follow TDD flow by first writing failing doc/fixture or focused repository checks proving `apps/signal-foundry/doc/financial-poc/enable-banking-pko.md` documents Enable Banking sandbox/production app setup, private key outside repo, app id as JWT `kid`, redirect/tunnel usage, restricted production linked-account activation, env vars, PKO/ASPSP discovery, AI-agent live test steps, and SCA limitation; proving `apps/signal-foundry/doc/financial-poc/monobank.md` documents token setup, env vars, 60-second limits, 31-day-plus-1-hour statement cap, default account `0`, AI-agent live test steps, and live-call warning; and proving `.gitignore` covers required financial POC session/export patterns before implementing docs/ignore updates and verifying focused tests.

## 5. Enable Banking Local HTTPS Callback Correction (`enable-banking-local-https-callback`)

- [ ] 5.1 Add HTTPS local callback support for Enable Banking `connect`, and must follow TDD flow by first writing failing focused tests proving `connect` posts an `https://.../callback` redirect URL, serves the callback listener over TLS, accepts the same code/state callback and state-mismatch failure path over HTTPS, loads and uses operator-supplied `--callback-cert-file` and `--callback-key-file` when both are provided, falls back to an ephemeral in-memory self-signed localhost/IP-SAN certificate only when neither file is provided, fails clearly instead of falling back when exactly one of the callback cert/key files is provided, warns on stderr for the self-signed fallback without leaking key material, keeps manual `start-auth`/`finish-session` behavior unchanged, and updates the Enable Banking POC documentation/tests to explain trusted local certificate guidance, required cert/key pairing, self-signed fallback limitations, and tunnel/manual alternatives before implementing and verifying focused tests.

## 6. Final Completion Checks (`finance-poc-final-checks`)

- [ ] 6.1 After section 5 is implemented and its focused tests pass, run `make affected-lint-test` from the repository root and check whether AGENTS.md needs updates because commands, workflows, or architecture changed.

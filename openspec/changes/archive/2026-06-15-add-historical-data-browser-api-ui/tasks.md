## 1. Runtime Data Browser Reads (`runtime-data-browser-reads`)

- [x] 1.1 Add raw payload browser query/result DTOs and validation in `runtime/data`, and follow TDD flow by writing failing tests for venue, optional instrument/timeframe/range filters, ingestion run ID, entity hint, endpoint/request type, default/max limit, cursor decoding, and deterministic validation errors before implementing and verifying focused tests.
- [x] 1.2 Add filtered raw payload metadata listing in the data store/service, and follow TDD flow by writing failing tests proving metadata-only results, filtering by venue/run/entity/request/timeframe/range, stable `receivedAt ASC, id ASC` ordering, deterministic pagination, and no response body exposure before implementing and verifying focused tests.
- [x] 1.3 Add raw payload detail preview reads, and follow TDD flow by writing failing tests proving lookup by ID, blob body read, deterministic preview byte limit, truncation flag, response size reporting, metadata mapping, and not-found behavior before implementing and verifying focused tests.
- [x] 1.4 Add candle-linked raw payload metadata lookup by full natural candle key, and follow TDD flow by writing failing tests proving `provenanceSource` and `provenanceIdentity` are required with venue/symbol/asset class/timeframe/start/end, missing provenance is a deterministic validation error, linked payloads only are returned for the exact provenance-bearing candle key, unlinked or missing older data returns an empty result when appropriate, and ordering is stable before implementing and verifying focused tests.

## 2. Backend Data API (`backend-data-api`)

- [x] 2.1 Extend `apps/signal-foundry/internal/api/http/v1routes.yaml` with `/api/v1/data/candles`, `/api/v1/data/raw-payloads`, `/api/v1/data/raw-payloads/{id}`, and `/api/v1/data/candle-raw-payloads` schemas using camelCase JSON, including required `provenanceSource`/`provenanceIdentity` on candle-linked evidence requests and candle responses, and follow TDD flow by adding failing generated-contract/controller tests for request validation and response mapping before regenerating route code and implementing handlers.
- [x] 2.2 Add a protected data controller and route registration, and follow TDD flow by writing failing tests proving all data endpoints require auth, valid candle requests call deterministic replay reads and return ascending chart-ready OHLC/provenance rows, exactly 10,000 requested candle intervals is accepted, more than 10,000 requested candle intervals returns `400 Bad Request` before replay reads, invalid venue/symbol/asset class/timeframe/range returns 4xx, and missing candles are not synthesized before implementing and verifying focused tests.
- [x] 2.3 Add raw payload API handlers, and follow TDD flow by writing failing tests proving list responses contain metadata only, support implemented filters/pagination, detail responses include bounded preview/truncation metadata, missing IDs return 404, candle-linked evidence maps required provenance-bearing natural-key query params to runtime reads, and omitted/blank provenance fields return `400 Bad Request` instead of ambiguous matching before implementing and verifying focused tests.

## 3. UI Data Browser (`ui-data-browser`)

- [x] 3.1 Add the typed data API client wrapper and `lightweight-charts` dependency, and follow TDD flow by writing failing tests for auth fetch usage, query parameter serialization, candle/raw/detail response mapping, and error handling before implementing and updating `package-lock.json`.
- [x] 3.2 Add protected `#/data` routing and authenticated nav entry, and follow TDD flow by writing failing tests proving unauthenticated users opening `/data` are redirected to login, authenticated nav shows `Chat / Data / Providers`, and the route renders the data browser shell before implementing and verifying focused tests.
- [x] 3.3 Add the data browser filters, summary, chart, candle selection, raw payload table, linked evidence panel, and detail drawer, and follow TDD flow by writing failing tests for required-field validation, optional client-side 10,000-interval cap messaging matching the server rule, no initial auto-query, loading/empty/error states, API calls with expected params, chart row mapping including `provenanceSource`/`provenanceIdentity`, table rendering, raw row detail fetch/drawer open, and candle selection loading linked evidence with the selected candle's provenance fields before implementing and verifying focused tests.
- [x] 3.4 Update `apps/signal-ui/ui-wireframe.md` for the new route, nav, layout, states, API behavior, and accessibility expectations, and follow TDD flow by keeping documentation changes with the UI behavior task and verifying affected UI tests still cover the documented states.

## 1. Finance Shell Foundation

- [ ] 1.1 Add a dedicated finance-route shell and shared route wiring, and must follow TDD flow by first adding failing UI route or component tests proving `#/finance`, `#/finance/tenants`, `#/finance/accounts`, `#/finance/accounts/:accountId`, `#/finance/connections`, `#/finance/connections/synthetic`, `#/finance/transactions`, `#/finance/transactions/new`, `#/finance/transactions/:transactionId`, `#/finance/categories`, `#/finance/imports`, and `#/finance/jobs/:jobId` render inside finance-specific chrome with a persistent desktop rail, compact utility row, supported finance destinations only, and no legacy `FinanceSubnav` card as the primary finance navigation before implementing the shared shell components, route composition, and shell portion of the manual smoke run/report/fix/rerun loop.
- [ ] 1.2 Centralize active-tenant shell context for tenant-aware finance routes, and must follow TDD flow by first adding failing route or store tests proving sole-tenant auto-selection, multi-tenant route-preserving selection, one shared shell-level tenant control reused across tenant-scoped finance routes, and direct finance deep links stay on the originally requested route after tenant resolution before implementing the tenant-context changes and verifying the tenant-selection portion of the manual smoke guide through the same sub-agent evidence loop.

## 2. Finance Dashboard Composition

- [ ] 2.1 Recompose `#/finance` into the planned finance-dashboard hierarchy, and must follow TDD flow by first adding failing page or component tests proving the route shows the finance dashboard header, reporting-range controls, route actions, KPI cards, cash-flow visual region, account summary, category or spending summary, recent transactions, sync activity, alerts or insights, and missing-FX diagnostics inside the new shell with correct loading, empty, and error states before implementing the dashboard layout and dashboard portion of the manual smoke run/report/fix/rerun loop.
- [ ] 2.2 Keep dashboard data usage derived from existing endpoints and document the new shell behavior, and must follow TDD flow by first adding failing client or page tests proving the dashboard composes its data from existing finance dashboard, accounts, transactions, and connections sources rather than new API contracts, then update `apps/signal-ui/ui-wireframe.md` alongside the implementation so the documented finance shell and dashboard states stay aligned with the tested behavior.

## 3. Transactions Browse Workspace

- [ ] 3.1 Rebuild `#/finance/transactions` as a table-first browse workspace, and must follow TDD flow by first adding failing page or component tests proving the route shows page-level actions, search or date or filter toolbar controls, summary chips, selectable ledger rows, the expected transaction columns, and browse-focused loading, empty, pagination, and error states using existing transaction, account, and category data sources before implementing the new ledger layout.
- [ ] 3.2 Add a responsive contextual inspector while preserving dedicated create and edit flows, and must follow TDD flow by first adding failing page or route tests proving selecting a transaction opens a right-side inspector on wide screens, the inspector exposes selected merchant, amount, type, metadata, category or status context, supported quick actions, notes or tags state, and narrow screens remain usable while `#/finance/transactions/new` plus `#/finance/transactions/:transactionId` stay the full-record mutation routes before implementing the inspector, route coordination, and transactions/responsive portions of the manual smoke run/report/fix/rerun loop.

## 4. Manual Smoke Documentation

- [ ] 4.1 Add the finance-shell smoke runbook and manual e2e index entry, and must follow TDD flow by first documenting the expected sign-in, active-tenant, dashboard, transactions, responsive, and visual smoke path in `docs/manual-e2e/finance-ui-shell-smoke-e2e.md` and linking it from `docs/manual-e2e/README.md`, then keep the guide aligned with the implemented and tested route behavior.

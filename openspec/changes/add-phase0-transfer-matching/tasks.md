The numbered groups are the ordered implementation chunks. After every group,
apply the design's mandatory OpenSpec shallow-review, commit, and deviation-
resolution gate before starting the next group. Isolated PostgreSQL manual E2E,
deep final review, user approval, and archive/submission remain completion gates in
`design.md`, not separate checkbox tasks.

## 1. Pair Persistence And Preservation

- [x] 1.1 Add `TransferMatchingExcluded` to the internal transaction domain, provider merge, and explicit-column GORM model as `transfer_matching_excluded boolean NOT NULL DEFAULT false`, and make shared ordinary transaction conflict updates preserve stored `kind`, `transfer_group_id`, `transfer_matched_at`, and exclusion while returning persisted pair values; must follow TDD flow by first adding failing randomized mapping/provider-merge/PostgreSQL tests for new-row defaults, existing linked and excluded rows surviving user edits and sync refreshes, preserved categories/tags/provider fields, returned saved state, and one shallow migration smoke, then implement without exposing the flag through OpenAPI and run focused finance checks plus the repository completion command.
- [x] 1.2 Add a dedicated `TransferPairStore` for narrow tenant-and-ID-scoped atomic link and unlink updates, migrate `LedgerService` off the legacy pair-save method while retaining all current broader manual validation, and set exclusion on both unlinked legs without clearing it on links; must follow TDD flow by first adding failing randomized PostgreSQL and Mockery-backed ledger tests for automatic/manual link fields, unequal/cross-currency/excluded manual links, categories/tags preservation, silent unlink to regular, all-or-nothing second-leg failure, missing-row rollback, and no insert or unrelated-field write, then implement, regenerate mocks with the module-standard Mockery command, and run focused finance checks plus the repository completion command.

## 2. Bulk Load And Matcher Service

- [x] 2.1 Add the pair store's one-call compact eligible projection load over the original range extended by 144 hours on both sides; must follow TDD flow by first adding failing randomized PostgreSQL behavior tests for tenant isolation, visible tenant accounts, booked status, regular/expense/income/unmatched-transfer kinds, manual/CSV/provider sources, categories, nonzero and nonexcluded state, absent pair fields, hidden/missing accounts and transactions, pending/refund/reconciliation/opening/system exclusion, exact half-open load bounds, selected projection fields, and one call without paging/truncation, then implement without matching generated SQL strings and run focused finance checks plus the repository completion command.
- [x] 2.2 Add the deterministic in-memory opposite-amount/currency/time index and mutual-uniqueness matcher that finishes all decisions before writes; must follow TDD flow by first adding failing randomized tests for exact signed integer opposites including minimum `int64`, same currency, different accounts, inclusive 72 hours, different offsets and DST, zero/one/multiple candidates, both one-to-many directions, hour-0/hour-72/hour-144 outer ambiguity on either boundary, in-range starting scope, outside partners, two outside legs, both starting signs, pair deduplication, shuffled-input determinism, cancellation, and no nearest/first tie-breaker, then implement and run focused finance checks plus the repository completion command.
- [x] 2.3 Add constructor-enforced `TransferMatchingService.Submit`/`Match` composition with strict range validation, one immutable load per attempt, pair-save execution, attempt counts, camelCase diagnostic context, and retry behavior; must follow TDD flow by first adding failing generated-Mockery tests for one bulk load and no candidate reads, all decisions preceding saves, matched/unmatched/ambiguous/skipped units, missing-row continuation, later database failure preserving earlier commits, partial failure logging, retry reload, completed-pair/exclusion absence, tenant membership, optional publication, and required dependencies, then expose the service through `finance.Finance`, regenerate mocks, and run focused finance checks plus the repository completion command.
- [x] 2.4 Preserve the existing category-classification and reporting behavior around newly matched pairs; must follow TDD flow by first adding failing focused finance/PostgreSQL behavior tests proving a preassigned category and tags survive matching, classification after matching skips the transfer, classification before matching does not block it, matched legs retain account-balance effects while leaving income/expense totals, and the exclusion flag has no reporting meaning, then implement only any required narrow integration corrections and run focused finance checks plus the repository completion command.

## 3. Explicit Command, API, Event, And Worker Lifecycle

- [x] 3.1 Add `finance.transfer-matching.explicit.v1` and explicit submission through the matching service and appdispatch adapter; must follow TDD flow by first adding failing tests for unchanged offset-bearing bounds, strict nonzero start-before-end validation, tenant membership, requester metadata, no inline match, no eager job row, publication failure, a fresh message ID for every new identical submission, and stable identity for redelivery of one published command, then implement and run focused finance/app checks plus the repository completion command.
- [x] 3.2 Register the command as the single observed `finance.transfer-matching` handler and add OpenAPI-first `POST /api/v1/finance/tenants/{tenantId}/transactions/match-transfers` with generated route code and a focused controller dependency; must follow TDD flow by first adding failing generated-Mockery handler and registered-route tests for authentication, tenant denial, full RFC 3339 offsets, malformed/equal/reversed bounds, exact service range mapping, `202` job-only response, lazy initial `404`, succeeded/failed lifecycle, malformed payload, terminal failure mapping, transport retry, and duplicate terminal delivery, then update `v1routes.yaml`, run apigen and Mockery generation from their documented roots, implement the controller/wiring, and run focused finance/app checks plus the repository completion command.
- [x] 3.3 Add the ordinary `finance.transfer-matching.v1` subscriber to existing `finance.bank-sync-window-completed.v1` deliveries without job observation; must follow TDD flow by first adding failing typed-event/subscriber and integrated PostgreSQL tests for exact tenant/range/message/source context, no account/source filter, parity with explicit matching semantics, both classification and matching consumer groups receiving one window independently, zero automatic matching jobs, duplicate delivery safety, committed data surviving matching failure, earlier-window delivery surviving a later sync failure, and transport retry/dead-letter propagation, then implement and run focused app checks plus the repository completion command.
- [x] 3.4 Extend normal and bounded worker composition to own the observed worker plus classification and matching routers and update backend worker guidance; must follow TDD flow by first adding failing worker-root and command-runtime tests for concurrent long-running execution, cancellation/router error propagation, startup-failure cleanup, all routers stopping before publisher/database closure, observed-first `--once` draining, both ordinary drains attempted despite either error, joined errors, and an emitted bank event reaching both enrichments without imposing order, then implement, update `apps/sumweave/AGENTS.md` to document the new router/drain shape, and run focused app checks plus the repository completion command.

## 4. Ledger Feedback And Verification Readiness

- [x] 4.1 Add finance UI client submission and reusable local-calendar matching-range behavior; must follow TDD flow by first adding failing faker/clock-backed tests for encoded tenant IDs, camelCase request and job-only response, today plus 29 preceding dates, visible inclusive boundaries, wider history, invalid/reversed dates, start-of-first-day and start-of-day-after-last conversion, full offset-bearing RFC 3339 values, and a DST range with different boundary offsets, then implement without adding a third-party dependency and run focused UI checks plus the repository completion command.
- [x] 4.2 Add the tenant ledger's Bootstrap-first **Match transfers** form, tenant-wide scope copy, existing job observation, matching-specific terminal feedback, and tenant-scoped terminal refresh; must follow TDD flow by first adding failing faker-backed page/component tests for selected-tenant requirements, independence from account/search/source/page filters, duplicate-submit disabling, initiating-ID-only initial `404`, queued/running/succeeded/failed states, retained Finance job link, exact success/failure copy, refresh after either terminal state, tenant changes during execution, rerun availability, and unchanged silent manual link/unlink detail behavior, then implement without route-local layout CSS, update the wireframe, perform the required independent desktop/narrow UI design-review and fix/re-review flow, and run focused UI checks plus the repository completion command.
- [x] 4.3 Align durable documentation and deterministic verification fixtures with the completed transfer-matching behavior; must follow TDD flow by first tightening any automated smoke assertions that own the documented API/UI behavior, then add and link a transfer-matching manual E2E guide covering isolated PostgreSQL, API-created manual fixtures, API-only initial job `404`, bounded worker delivery, explicit and automatic committed-window paths, classification independence, matching/ambiguity/range boundaries, correction exclusions, reporting preservation, rerun/redelivery/partial-failure behavior, headed desktop/narrow ledger smoke, evidence under `tmp/`, and synthetic sync only for real event coverage, update enrichment implementation-status references, and run affected documentation-linked/UI checks plus the repository completion command.

## 5. Deterministic E2E Fixture Correction

- [x] 5.1 Correct deterministic E2E fixture mechanics only; must follow the verification-first/TDD flow by first retaining checks for the intentional public zero-amount rejection and the unchanged zero-amount ineligibility requirement, then create only the zero-amount eligibility fixtures directly in the isolated PostgreSQL test database through a safe deterministic, tenant-scoped fixture step while keeping other fixtures on supported public APIs, verify those rows through public matching and transaction-read APIs without exposing or allowing zero amounts through HTTP, compare reporting before and after with the fixed June custom RFC 3339 range rather than `current_month` or date-only values, syntax-validate every guide shell/Python script, and rerun the complete isolated E2E round including headed desktop/narrow smoke without changing product behavior or broadening scope.
- [x] 5.2 Correct the transfer-matching manual E2E guide's API-only process lifecycle only; make startup/cleanup own and terminate the actual launched server process or process group, verify no listener remains on port 4501 before PM2 restoration, then verify PM2 `api`, `worker`, and `ui` are online with a healthy `/health`, and rerun the complete deterministic E2E round from a fresh isolated database (including headed desktop/narrow smoke), not only the handoff, completing 5.1 and 5.2 only when the full round is clean; do not change product behavior or general PM2 workflow documentation unless required by this guide defect.

## 6. Final Review Blocker Follow-Up

- [ ] 6.1 Correct initiating-tenant terminal ledger refresh/invalidation with the smallest UI change; must first add a failing focused UI test that exercises submitting in tenant A, switching the active tenant to B before the matching job becomes terminal, and then completing the job for both success and failure terminal outcomes as applicable, proving tenant A is refreshed or invalidated for when it is revisited without refreshing or mutating tenant B and while preserving the terminal result feedback and rerun availability, then implement the minimal fix, run the focused UI test/checks and the root `make affected-lint-test`, and complete the required shallow review before commit.

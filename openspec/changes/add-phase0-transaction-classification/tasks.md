The numbered groups are the ordered implementation phases. After every group,
apply the design's mandatory OpenSpec shallow-review, commit, and deviation-
resolution gate before starting the next group. Synthetic manual E2E loops, deep
final review, user approval, and archive/submission remain completion-protocol
gates described in `design.md`, not separate checkbox tasks.

## 1. Rules And Matching Foundation

- [x] 1.1 Add classification rule domain types, the explicit-column `finance_classification_rules` GORM model and indexes, the existing migrator registration, and a dedicated transactional rule store for ordered list/filter, append, replace, gap-closing delete, and adjacent move; must follow TDD flow by first adding failing randomized persistence tests for ordering, duplicate contents, tenant/category filtering, one-based positions, edge no-ops, transaction rollback, and one shallow migration smoke, then implement and run focused finance checks plus the repository completion command.
- [x] 1.2 Add the focused classification-rule service and make every category-hide path depend on its narrow live-reference guard while preserving logical hide history; must follow TDD flow by first adding failing Mockery-backed service tests for membership, same-tenant visible categories, match type and nonblank condition validation, position preservation, reference IDs, successful hide, and typed blocked-hide failure, then implement, update all constructor/composition callers, and run focused finance checks plus the repository completion command.
- [x] 1.3 Add the internal first-match description matcher and dedicated classification transaction store with 200-row ID-keyset selection and scoped conditional category updates; must follow TDD flow by first adding failing randomized tests for case and surrounding-space handling, literal contains, internal whitespace/punctuation, blank descriptions, rule precedence, all four eligible kinds, required manual/CSV/provider source inclusion, excluded states/kinds/ranges/tenants, visible-category lookup, and conditional-write races, then implement without matching generated SQL strings and run focused finance checks plus the repository completion command.
- [x] 1.4 Add the focused classification execution service, attempt counts/log context, terminal missing-category behavior, and `finance.Finance` composition; must follow TDD flow by first adding failing Mockery-backed tests proving rules load once, batches advance by ID, unmatched/skipped/classified semantics, counts advance only after commit, partial failure is retained, retry reloads rules and skips existing categories, and required dependencies are constructor-enforced, then implement and run focused finance checks plus the repository completion command.

## 2. Explicit Command And Backend API

- [x] 2.1 Add the `finance.classification.explicit.v1` contract and authenticated classification submission path through the finance service and appdispatch adapter; must follow TDD flow by first adding failing tests for unchanged offset-bearing bounds, strict start-before-end validation, tenant membership, requester metadata, no inline execution, no eager job row, returned message identity, and retry-safe publication behavior, then implement and run focused finance/app checks plus the repository completion command.
- [x] 2.2 Register the explicit command as the single observed `finance.classification` handler and map it to the shared classifier with existing terminal-versus-transport failure behavior; must follow TDD flow by first adding failing generated-Mockery handler and worker integration tests for metadata, identical range mapping, lazy job materialization, succeeded/failed lifecycle, malformed payload, infrastructure retry, and duplicate terminal delivery, then implement and run focused app checks plus the repository completion command.
- [x] 2.3 Add OpenAPI-first rule CRUD/move/filter, category-delete conflict, and explicit transaction-classify routes with focused controller dependencies and generated apigen output; must follow TDD flow by first adding failing registered-route tests for authentication, tenant isolation, request/response/status contracts, full RFC 3339 offsets, invalid ranges, minimal mutation responses, and the documented `409` rule ID body, then implement controllers and error mapping, regenerate routes, and run focused finance/app checks plus the repository completion command.

## 3. Committed-Window Automatic Classification

- [x] 3.1 Add the finance-owned bank-sync-window completion fact and narrow transaction-bound publisher seam to requested-window apply so ledger writes, successful checkpoint, and event publish atomically; must follow TDD flow by first adding failing Mockery-backed window-store and PostgreSQL persistence tests for exact tenant/connection/range/source IDs, successful commit, publication rollback, checkpoint rollback, early committed windows surviving later failure, and no event on fetch/apply failure, then implement and run focused finance checks plus the repository completion command.
- [x] 3.2 Add the app `appevents` adapter and ordinary `finance.classification.v1` subscriber that maps `finance.bank-sync-window-completed.v1` to the same classifier without job observation; must follow TDD flow by first adding failing typed-event and subscriber tests for serialization, exact range parity with the explicit handler, source logging context, duplicate delivery safety, terminal logging, retry/dead-letter propagation, and zero job records, then implement and run focused app checks plus the repository completion command.
- [x] 3.3 Compose the observed worker and ordinary classification router under one worker root with ordered bounded draining and database-safe shutdown; must follow TDD flow by first adding failing worker-root and command-runtime tests for concurrent long-running execution, `--once` draining bank jobs before their emitted events, cancellation/error propagation, restart recovery, and closing both routers before publisher/database release, then implement and run focused app checks plus the repository completion command.

## 4. Rules Management And Manual-Assignment UI

- [x] 4.1 Add finance client types and methods for rule CRUD/move/filter, category removal, and typed category-reference conflicts; must follow TDD flow by first adding failing faker-backed client tests for encoded tenant/rule/category IDs, camelCase payloads, minimal rule and category-removal statuses, ordered response mapping, and safe `409` rule-ID decoding without broad error-body exposure, then implement and run focused UI checks plus the repository completion command.
- [x] 4.2 Add protected `#/finance/rules`, active-tenant and document-title behavior, Finance shell navigation/breadcrumbs, a Bootstrap-first ordered rule manager with target category names and recoverable CRUD/move states, and the existing `FinanceCategories` page's category-removal action with recoverable pending/error states and successful list refresh; must follow TDD flow by first adding failing route, shell, rules-page, and Categories-page tests for protection, tenant reuse, load/empty/error states, append/edit/delete, edge controls, mutation refetch, order precedence visibility, removal success, no hide on `409`, conflict copy that explains rules must be retargeted or deleted, and identifying or linking the returned rule IDs to the filtered rules view, then implement without route-local layout CSS, update the wireframe for both Rules and Categories removal/conflict states, perform the required desktop and narrow-screen visual verification flow for both pages, and run focused UI checks plus the repository completion command.
- [x] 4.3 Offer separate optional rule creation after successful inline or full-editor category assignment while keeping the saved transaction independent; must follow TDD flow by first adding failing faker-backed component tests for current-description/category prefill, exact/contains choice, editable condition, explicit save, cancel, rule-save failure, transaction-save failure, and no offer after category clear, then implement across the shared and detail transaction editors, perform the required visual verification flow, and run focused UI checks plus the repository completion command.

## 5. Explicit Feedback UI And Verification Readiness

- [ ] 5.1 Add the rules-page explicit classification panel and local-calendar range conversion; must follow TDD flow by first adding failing clock-controlled tests for today plus 29 preceding dates, visible boundaries, reversed-range rejection, wider history, inclusive end conversion, full RFC 3339 serialization, and a DST range whose boundaries have different offsets, then implement with native date controls and run focused UI checks plus the repository completion command.
- [ ] 5.2 Integrate the returned classification ID with existing observed-dispatch polling, terminal feedback, Finance job links, and ledger refresh signaling; must follow TDD flow by first adding failing faker-backed page tests for initiating-ID-only `404` pending behavior, queued/running/succeeded/failed states, retained detail link, retry submission, partial-success failure copy, and post-success refresh behavior, then implement, perform the required desktop/narrow visual verification flow, and run focused UI checks plus the repository completion command.
- [ ] 5.3 Align user-visible documentation and deterministic synthetic verification fixtures with the completed classification flows; must follow TDD flow by first tightening automated smoke assertions around the documented API/UI behavior, then update the Finance wireframe and synthetic provider API/UI runbooks for ordered rules, automatic committed-window classification, explicit classification, repeated-category safety, evidence capture under `tmp/`, and clean rerun expectations, and run affected documentation-linked/UI checks plus the repository completion command.

## 1. PostgreSQL Environment Provisioning

- [x] 1.1 Establish `make postgres-bootstrap`, the canonical Compose environment, and checked-in local/test config for `sumweave_local` and `sumweave_test` with database-owner, migrator, and runtime roles, the fixed prefix contract, both explicit migration runs, and post-migration runtime grants; define privileged `POSTGRES_BOOTSTRAP_DSN` with the Compose administrator default and require it without a default in external mode for readiness, fresh role/database creation, ownership, grants, and migrator default privileges, following TDD flow by first adding failing setup/config contracts and proving missing/insufficient external privilege fails before migration, the test migration is the one shallow serialized migration smoke, and cluster SQL contains no application schema definitions.
- [x] 1.2 Establish database-free routine targets plus serial root `postgres-test-runtime`, `postgres-test-finance`, `postgres-test-sumweave`, and `postgres-verify` targets using module `make test-postgres`, the `postgres_test` tag, `SUMWEAVE_POSTGRES_TEST_DSN`, and a shared bootstrap prerequisite; make each module routine target write `.cover/routine.out` and apply `.testcoverage-routine.yaml`, make each tagged target write `.cover/postgres.out` and apply the full `.testcoverage.yaml`, retain 90% file/total thresholds in both, allow only exact anchored routine omissions paired to tagged files covered at 90% in the full profile, and forbid broad exclusions or lowered assertions; preserve root `make test` aggregation by consuming core `routine.out` profiles and unchanged template `profile.out` profiles into root `.cover/profile.out`, then run its existing HTML/report and `go-test-coverage --profile .cover/profile.out` aggregate check; follow TDD flow by first making exact root/module target, profile, config, threshold, ownership, aggregation, and root-check assertions fail, then prove routine `make test`/Nx targets and root `make test` neither probe nor skip on database availability and root routine tests plus aggregation succeed without PostgreSQL, while each focused PostgreSQL target bootstraps when invoked alone.
- [x] 1.3 Add the separate `workflow_dispatch` PostgreSQL verification workflow using Ubuntu `postgresql.service`, `pg_isready`, the Ubuntu `postgres` OS account to establish an ephemeral administrator password, and an exported privileged `POSTGRES_BOOTSTRAP_DSN`, then invoke `make postgres-verify` with `POSTGRES_MANAGED_EXTERNALLY=1`, host `127.0.0.1`, and port `5432` while leaving reusable `tests-run.yml` database-independent; follow TDD flow by first adding failing workflow contracts, then prove this path performs complete bootstrap and both migrations from a fresh service without Compose and no routine workflow or target invokes PostgreSQL setup.

## 2. Generic Runtime PostgreSQL Cutover

- [x] 2.1 Convert runtime persistence/composition tests to the `postgres_test` lane and prepared `sumweave_runtime_` tables through `SUMWEAVE_POSTGRES_TEST_DSN`, with runtime-role connections, no per-test AutoMigrate, fresh randomized users/sessions/profiles/providers, scoped reads, and only narrowly serialized demonstrated conflicts, while retaining database-free unit coverage in routine tests; follow TDD flow by moving each existing behavior case before changing its fixture, classify only exact irreducibly PostgreSQL-backed source files as routine omissions, and require both `make test` routine coverage and `make postgres-test-runtime` full tagged coverage to pass their 90% file/total checks.
- [x] 2.1A Before runtime task 2.2, extract the app runtime-database compatibility slice from task 4.1: move the Engine, `cmd/sumweave` main and runtime-resolution, application-composition database-runtime, and wireup migration/jobs/HTTP cases that construct runtime database storage to the `postgres_test` lane; use checked-in `test.yaml`, the bootstrap-prepared application and `sumweave_runtime_` schemas, fixed prefixes, runtime-role suite DSNs, randomized/scoped state, and no test-owned migration, while retaining their database-free cases in the routine lane and retiring detailed per-test migration assertions where the task 1.1 bootstrap smoke owns them; follow TDD flow by first exposing the routine failure against PostgreSQL-only runtime construction, then require `make postgres-test-sumweave` and PostgreSQL-unavailable `make affected-lint-test` to pass both unchanged 90% coverage gates without new exclusions or lower thresholds. This prerequisite owns only the listed app fixtures and is not completed again by task 4.1.
- [ ] 2.2 Convert runtime sessions, provider configuration, agent profiles, GORM predicates, dialector, and SQL connection construction to PostgreSQL and remove runtime SQLite branches and files without adding timestamp normalization; follow TDD flow by preserving each affected behavior in the appropriate routine or tagged lane before simplifying production code, then run the repository completion protocol and focused runtime PostgreSQL target after task 2.1A is independently reviewed and committed.

## 3. Finance PostgreSQL Cutover

- [ ] 3.1 Convert finance persistence/composition tests to the `postgres_test` lane and prepared `finance_` tables through `SUMWEAVE_POSTGRES_TEST_DSN`, with runtime-role connections, no per-test migration, fresh randomized tenants, tenant-scoped data, and only narrowly serialized demonstrated conflicts, while retaining database-free business coverage in routine tests; follow TDD flow by moving each behavior case before changing its fixture, classify only exact irreducibly PostgreSQL-backed source files as routine omissions, and require both `make test` routine coverage and `make postgres-test-finance` full tagged coverage to pass their 90% file/total checks.
- [ ] 3.2 Simplify finance SQL/GORM construction and dialect-specific predicates to PostgreSQL-only behavior and remove finance SQLite connection code without changing date or timestamp normalization; follow TDD flow by updating constructor, persistence, and composition expectations in the appropriate lane before deleting each dialect branch, then run the repository completion protocol and focused finance PostgreSQL target.

## 4. Sumweave Application PostgreSQL Cutover

- [ ] 4.1 Complete the remaining app test conversion after the runtime-database fixture slice owned by task 2.1A: move the still-routine database-backed command/config, auth, jobs, finance registration, controller, Engine, and wireup persistence/composition cases to the `postgres_test` lane using checked-in `test.yaml`, prepared fixed prefixes, runtime-role connections, no per-test migration, randomized identities, and scoped reads, while leaving database-free command/config behavior in routine tests; do not repeat or claim completion for the Engine, `cmd/sumweave` main/runtime-resolution, application-composition database-runtime, or wireup migration/jobs/HTTP fixture work completed in task 2.1A; follow TDD flow by moving each remaining behavior case before changing its fixture, classify only exact irreducibly PostgreSQL-backed source files as routine omissions, and require both `make test` routine coverage and `make postgres-test-sumweave` full tagged coverage to pass their 90% file/total checks.
- [ ] 4.2 Make app SQL connections, authentication, job-projection persistence, finance persistence wiring, and dialect-specific predicates PostgreSQL-only without changing date or timestamp normalization; follow TDD flow by preserving observed-consumer job projection, finance-owned bank schedule and daily FX due-state tables, `jobs enqueue-due` publication/state advance without eager job rows, and the shared prepared-schema behavior of `start`, `start-all`, `jobs worker`, and `jobs enqueue-due` before deleting DSN classification, SQLite dialectors, and SQLite query variants, then run the repository completion protocol and focused app PostgreSQL target.
- [ ] 4.3 Make appdispatch publication, subscription, offsets, and explicit schema migration unconditionally use the existing PostgreSQL Watermill adapters; follow TDD flow by preserving immutable message IDs, topic/consumer-group offsets, transport fan-out, same-group coordination, restart resume, transaction, retry, dead-letter, lifecycle, split-mode prepared-schema behavior, and no startup migration in the tagged PostgreSQL lane before deleting the custom SQLite transport and multi-driver selection, then run the repository completion protocol and focused app PostgreSQL target.

## 5. Dependency And Contract Cleanup

- [ ] 5.1 Remove SQLite drivers and their transitive dependency tree from `runtime`, `finance`, and `apps/sumweave`, then synchronize workspace sums without adopting or behaviorally changing template tools or the integration harness; follow TDD flow by first adding a failing core-module dependency check, then tidy only core modules, accept only mechanical `go work sync` changes elsewhere, and report any independent out-of-scope SQLite import instead of expanding scope.
- [ ] 5.2 Align active architecture, module guidance, local setup, PM2 workflow, manual E2E guides, and database plans with mandatory local Compose PostgreSQL, the concrete role/database/prefix/bootstrap contract, database-independent routine CI, tagged non-routine verification, two explicit migration runs, no SQLite data migration, and no timestamp normalization change; follow TDD flow by updating documentation/config assertions first, preserve archived OpenSpec history, run strict OpenSpec validation, and complete both mandatory `make affected-lint-test` and applicable focused `postgres-*` checks as part of this cleanup rather than as a separate verification task.

## 1. Completed PostgreSQL-Only Cutover (Historical)

- [x] 1.1 Add canonical Compose/bootstrap setup with separate local and test databases, explicit migrations, and runtime grants.
- [x] 1.2 Convert core database fixtures from SQLite to PostgreSQL and add `postgres_test` build tags.
- [x] 1.3 Remove runtime, finance, app, auth, jobs, and appdispatch SQLite production implementations.
- [x] 1.4 Remove direct SQLite dependencies from the core product modules and align active PostgreSQL-only configuration/docs.
- [x] 1.5 Implement the now-rejected separate PostgreSQL verification and coverage lanes; correction tasks below supersede that test workflow.

## 2. Correction: One Normal PostgreSQL Test Flow

- [x] 2.1 Add the ordinary CI PostgreSQL prerequisite by placing exactly one `make postgres-bootstrap` step in `.github/workflows/tests-run.yml` immediately before its unchanged Nx test step. TDD is not applicable because this changes workflow setup rather than product behavior; validate the workflow shape, run `make postgres-bootstrap`, and run `make affected-lint-test` in this same chunk. Land this before, or atomically with, task 2.2 so no reviewed commit selects tagged tests without preparing PostgreSQL.

- [x] 2.2 Switch all ordinary backend tests and their command documentation atomically to one PostgreSQL-backed flow. TDD applies only to closing the demonstrated finance migrator coverage gap: in this same chunk add the smallest shallow `postgres_test` smoke to the neutral pre-change owner `finance/persistence/migrations_test.go`, execute the concrete migrator against the bootstrap-prepared test database, and assert only successful migration; do not alter product code, restore detailed schema contracts, or add another migration-test owner. Restore the pre-change Makefile/profile organization with the smallest change, then run `make postgres-bootstrap`, `make affected-lint-test`, and root `make test`, and audit target expansion plus command-text residue; keep this task unchecked until all are green. Restore the root, runtime, finance, and app Makefiles to one `test` target using the standard test DSN, `-tags=postgres_test`, `.cover/profile.out`, and `.testcoverage.yaml`; preserve root Nx parallelism and retain finance `-p 1` only for a reproduced conflict. Delete `.github/workflows/postgres-verify.yml`, module `test-postgres`, root `postgres-test-*`/`postgres-verify` and lane-contract targets, all three `.testcoverage-routine.yaml` files, and caller-side routine/PostgreSQL profiles and finance migration coverage wiring, including `SUMWEAVE_FINANCE_MIGRATION_COVER_DIR` exports, raw-covdata composition, and marker checks in Makefiles/configs. In this same commit update `AGENTS.md`, `runtime/AGENTS.md`, `finance/AGENTS.md`, and `apps/sumweave/AGENTS.md`; restore the lane-only `build/AGENTS.md` rule; and correct command-facing `docs/ARCHITECTURE.md`, `docs/database-backed-state-plan.md`, `docs/manual-e2e/postgres-local-verification.md`, and `apps/sumweave/doc/architecture.md` so no active instruction advertises removed commands or database-free ordinary backend tests. Preserve `tests/AGENTS.md` agent-harness wording.

- [x] 2.3 Reduce PostgreSQL scripts to the operational Compose bootstrap path. TDD is not applicable to deleting rejected contract/parser scripts and restoring the bootstrap script's normal branch; after the restoration, use shell syntax checks, bootstrap a clean Compose volume twice to prove fresh and idempotent setup, then run `make affected-lint-test`. Delete `scripts/postgres/bootstrap-contract-test.sh`, `targets-contract-test.sh`, `workflow-contract-test.sh`, `documentation-contract-parser.py`, `documentation-contract-test.sh`, and `scripts/core-dependencies-contract-test.sh`. In `scripts/postgres/bootstrap.sh`, remove the external/manual lane and own all script-side removal of `SUMWEAVE_FINANCE_MIGRATION_COVER_DIR`, `GOCOVERDIR`, migration raw-covdata directories, and readiness-marker creation; retain normal Compose role/database setup, both explicit migrations, runtime grants, `compose.yaml`, and the three SQL files directly used by bootstrap. Update only bootstrap implementation prose not already made command-true by task 2.2.

- [x] 2.4 Restore runtime test filenames and ownership while preserving retained PostgreSQL behavior. Follow TDD for retained behavior by ensuring each moved case passes in its destination before deleting the split source; pure filename reversal and formatting restoration need no new test. Reverse the five exact runtime `_postgres_test.go` renames, merge agent/session splits into pre-change owners, and use a neutral shared fixture name only where no owner exists; preserve `postgres_test` tags, runtime-role fixtures, randomized identities, scoped assertions, and only demonstrated isolation. Run `make postgres-bootstrap`, `make -C runtime test`, and `make affected-lint-test`.

- [x] 2.5 Restore root `finance` test ownership and remove duplicate lane coverage. Follow TDD for retained PostgreSQL behavior by moving existing assertions before deleting their split owners; pure hunk restoration needs no new test. Merge `root_service_postgres_test.go` into `root_service_test_helper_test.go`, remove `imports_parser_test.go` only when the restored tagged imports owner covers it, restore branch-only mock churn, and preserve required tags, fixtures, randomization, scoping, and behavior in all touched root/fixture tests. Run `make postgres-bootstrap`, `make -C finance test`, and `make affected-lint-test`.

- [x] 2.6 Restore `finance/persistence` test and migration ownership without a production mock seam. Retain the single shallow tagged smoke already added by task 2.2 in `migrations_test.go`; in that same `TestMigrate` owner, add only one canceled-context error assertion using the prepared real PostgreSQL database. Keep `Migrator` concrete and preserve its two ordered model groups and exact wrapped error contexts by deduplicating the direct `WithContext(ctx).AutoMigrate(...)` call and wrapping in one unexported concrete method; do not add an interface, mock, second smoke, schema assertion, fixture, test owner, coverage exclusion, or lower either 90% gate. Seam/mock/replacement-suite deletion is restoration and needs no other test. Preserve PostgreSQL fixture/tag/predicate changes, remove duplicate `instant_predicate_test.go` when covered, and remove `autoMigrator`, its generated mocks, and `migrator_test.go`. This task never restores bootstrap instrumentation or caller-side coverage transport. Run `make postgres-bootstrap`, `make -C finance test`, and `make affected-lint-test`.

- [x] 2.7 Restore `apps/sumweave/cmd/sumweave` concrete command code and pre-change test ownership. Follow TDD for retained command and PostgreSQL validation behavior by moving/confirming its assertions before removing resolver, focused-service, and `Prepare` seams; generated-mock and split-file restoration needs no new test. Remove those seams from `finance_cmd.go`, `main.go`, and `user_cmd.go` with their mocks/replacement tests, merge retained PostgreSQL cases into pre-change owners, merge `main_file_test.go` into `main_test.go`, and restore unrelated small-test churn. Run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.8 Restore root `apps/sumweave` Engine test ownership. Follow TDD for retained PostgreSQL Engine behavior by moving its existing assertions into `engine_test.go` before deleting `engine_file_test.go`; pure coverage-split restoration needs no new test. Preserve only required PostgreSQL Engine behavior and run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.9 Restore `apps/sumweave/internal` direct PostgreSQL composition and pre-lane tests. Follow TDD for retained validation/construction/migration behavior by confirming its destination assertions before removing coverage-only service/config/component factories; restoration of generated/shared coverage files needs no new test. Remove the factories from `agent_runtime.go` and `database_migrate.go`, merge `application_composition_postgres_test.go`, and restore `final_composition_coverage_test.go` and `mocks_test.go` to their pre-lane responsibilities. Run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.10 Restore `internal/appdispatch` to direct PostgreSQL transport/migration with neutral test ownership. Follow TDD for retained PostgreSQL behavior by moving the minimum real transport assertions before removing test adapters; SQL-mock replacement coverage and hunk restoration need no new test. Remove migration/publisher adapters and generated mocks, merge `router_postgres_test.go`, restore `import_boundary_test.go` fully to `main`, and restore branch-only logging/formatting/build-tag/test churn while keeping the SQLite transport deleted. Explicitly delete `appdispatch_routine_test.go` and its branch-only SQL-mock coverage cases unless one identified case is demonstrated as the minimal test of retained PostgreSQL behavior; if so, move only that case into `appdispatch_test.go` or `transport_edges_test.go` and still delete `appdispatch_routine_test.go`. Run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.11 Restore `internal/appevents` typed appdispatch behavior without its coverage seam. Follow TDD for retained adapter behavior by confirming it in the restored `events_test.go` owner before removing the registrar seam; generated/replacement test deletion is restoration and needs no new test. Remove the seam, mocks, replacement suite, and generic coverage-ignore churn, then run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.12 Restore `internal/auth` concrete token/store behavior while retaining PostgreSQL stores. Follow TDD for meaningful PostgreSQL rejection and store behavior by preserving their assertions before removing the random-reader seam; validation-suite and generated-mock rollback needs no new test. Restore pre-change owners hunk-by-hunk, then run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.13 Restore `internal/jobs` concrete PostgreSQL store/worker behavior and neutral tests. Follow TDD for retained PostgreSQL DDL/persistence behavior by moving real tagged assertions before removing schema/router factories; the unrelated periodic-cycle feature and coverage suites are pure rollback and require no new test. Remove generated mocks, database-free replacements, and split ownership; unconditionally revert `Worker.runPeriodicRecoveryCycle`, its router factory, and `worker_unit_test.go`. Run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.14 Restore `internal/financeapp` and `internal/api/http/v1controllers` test ownership without coverage-only mocks. Follow TDD for retained registration and synthetic-link PostgreSQL behavior by moving existing assertions into `register_test.go`, `finance_test.go`, or a neutral responsibility owner before deleting split files; generated/build-tag hunk restoration needs no new test. Restore concrete publisher/composition and controller mocks hunk-by-hunk without behavior changes, then run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.15 Restore `internal/wireup` direct composition and pre-change package tests. Follow TDD for retained PostgreSQL composition and its one explicit migration path by confirming destination assertions before removing config/component/schedule factories; mock, split-file, and formatting restoration needs no new test. Remove package-owned generated mocks/replacement tests, merge command-root/HTTP/jobs/migration cases, and restore formatting-only `finance.go`/`http.go` hunks. Run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.16 Restore ancillary app test and generated-mock churn in `internal/config`, `internal/api/http` and `middleware`, `internal/app`, `internal/infrastructure/httpclient`, and `internal/telemetry`. TDD is not applicable because these are provenance-confirmed test-formatting, branch-added coverage-case, or regeneration-only restorations with no production behavior change; retain local/test PostgreSQL DSNs and any independently required interface output, then immediately run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`. This chunk owns `ui_assets_test.go`, branch-added `otel_test.go` cases, `mock_slog_handler_test.go`, and the generated mocks in the named packages.

- [x] 2.17 Reconcile app and finance Mockery configuration after every consumer seam rollback. TDD is not applicable to generated-output reconciliation; remove only obsolete lane entries, regenerate only mocks required by retained interfaces, inspect every generated diff, then run `make postgres-bootstrap`, both `make -C finance test` and `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.18 Reconcile dependencies in an order that cannot recreate non-core churn. TDD is not applicable to manifest restoration, but dependency behavior must be verified immediately: tidy `runtime`, `finance`, and `apps/sumweave` in module-local mode first; run workspace synchronization only if required; restore `tests/agent/integration-cli` and `tools/{firecrawl,skills,workspacefs}` manifests/sums exactly to `main` last; then inspect every core/non-core manifest and `go.work.sum`. Use `go mod why` to permit SQLite metadata only where ADK test-only residue is proven outside production traversal, and prove core direct imports/production dependency graphs contain no SQLite driver without recreating a contract script. After the final manifest state, run `make postgres-bootstrap`, `make -C runtime test`, `make -C finance test`, `make -C apps/sumweave test`, and `make affected-lint-test`.

- [x] 2.19 Finish only uncoupled prose and active-change/base-spec cleanup. TDD is not applicable to documentation and exact base-spec restoration; audit remaining setup/product/manual-E2E prose hunk-by-hunk, preserve truthful PostgreSQL-only and agent-harness guidance, and restore `openspec/specs/database-migration-command/spec.md`, `openspec/specs/domain-event-pubsub/spec.md`, and `openspec/specs/finance-management/spec.md` exactly to `main` while leaving requirements in this active delta. Run `npx openspec validate use-postgresql-only --strict`, audit the checklist and the docs/base-spec portion of `git diff main...HEAD`, and confirm no unrelated or stale lane prose remains. This is not deferred implementation verification: tasks 2.1–2.18 own their checks, and the independent final reviewer owns the deep final-state review.

- [x] 2.20 Remove only the three stale indirect requirements `github.com/ncruces/go-strftime`, `modernc.org/mathutil`, and `modernc.org/memory` from `apps/sumweave/go.mod` by running module-local `GOWORK=off go mod tidy`; do not hand-edit the manifest, run workspace synchronization, or change any other `go.mod`, `go.sum`, or `go.work.sum`. TDD is not applicable to this dependency-only correction. Immediately inspect the tidy diff and require it to contain only those three removals, then run module-local `GOWORK=off go mod tidy -diff` and `GOWORK=off go mod verify` for `runtime`, `finance`, and `apps/sumweave`. After those checks pass, run `make postgres-bootstrap`, `make -C apps/sumweave test`, and `make affected-lint-test`, and confirm the final manifest diff contains no broader changes before checking this task.

## 3. Correction: Normal Go Test Selection

- [x] 3.1 Remove all 84 `postgres_test` occurrences from the same 76 core test files: 11 under `runtime/`, 44 under `finance/`, and 21 under `apps/sumweave/`. Remove the build constraint entirely from the 73 files where it is the only condition; change `runtime/agent/agent_profiles_test.go`, `runtime/agent/providers_config_test.go`, and `apps/sumweave/engine_test.go` from `postgres_test && !release` to `!release` so the unrelated release exclusion remains intact. In those same files, remove `postgres_test` from the seven stale missing-DSN diagnostics and rename the one GORM callback registration key neutrally; do not rename or split tests, change package ownership, alter fixtures or assertions, introduce another build tag, or add an alternate lane. The existing tagged ordinary flow already co-compiles these files with the untagged package tests, so duplicate helpers or test names are not expected; if normal no-tag selection nevertheless exposes a helper, name, or package conflict, resolve only that concrete conflict minimally in its existing package and owner. TDD is not applicable because this changes test selection and stale test-only labels rather than product behavior. In the same serialized chunk, remove only `-tags=postgres_test` from the runtime, finance, and app `go test` recipes and remove finance lint's `--build-tags=postgres_test`; preserve DSN injection, timeouts, shuffle/failfast, coverage scope/profile/gates, root aggregation/Nx parallelism, and any existing finance serialization. Replace only the stale positive `postgres_test` wording in root `AGENTS.md`, `runtime/AGENTS.md`, `finance/AGENTS.md`, and `apps/sumweave/AGENTS.md`; do not add a duplicate root rule or rewrite unrelated docs, specs, or `tests/AGENTS.md`. Before checking this task, prove no core `*.go` file mentions `postgres_test`, no core Makefile contains a PostgreSQL test/lint tag flag, no active documentation or base spec claims that the tag remains, and the three named files retain exactly `//go:build !release`; run `make postgres-bootstrap`, `make -C runtime test`, `make -C finance test`, `make -C apps/sumweave test`, root `make test`, and `make affected-lint-test`; then run `npx openspec validate use-postgresql-only --strict` and inspect the scoped diff for only this correction.

## 4. Correction: Environment-Owned Test DSN

- [ ] 4.1 Move the shared PostgreSQL test-process DSN out of module Makefiles and into the established execution-environment setup. TDD is not applicable because this changes shell/CI configuration propagation rather than product behavior. Add the runtime-role `sumweave_test` DSN as exported `SUMWEAVE_POSTGRES_TEST_DSN` in root `.envrc`, and set the same non-secret local Compose value through `env` on the existing Nx test step in `.github/workflows/tests-run.yml`; do not make bootstrap emit/export it or require manual CI settings. Remove the `SUMWEAVE_POSTGRES_TEST_DSN ?= ...` definitions and DSN assignment prefixes from `runtime/Makefile`, `finance/Makefile`, and `apps/sumweave/Makefile`, preserving `TZ`, timeouts, shuffle/failfast, coverage scope/profile/gates, and all target shapes. Do not change root `Makefile`, `compose.yaml`, `scripts/postgres/bootstrap.sh`, Nx project files, Go test helpers, app config Go code, or `apps/sumweave/internal/config/{default,local,test}.yaml`: bootstrap continues to prepare databases only, test helpers continue requiring the inherited test-process variable, `test.yaml` continues owning app-internal typed test-profile DSNs, and standard `APP_` `AutomaticEnv` mapping remains unchanged. In the same serialized chunk, verify the direnv environment exposes the variable; bootstrap PostgreSQL; run runtime, finance, and app module tests, root `make test`, the ordinary Nx test path with its cache bypassed to exercise child-process inheritance, and `make affected-lint-test`; inspect dry-run recipes to prove no module Makefile fallback or command-level DSN assignment remains; run `npx openspec validate use-postgresql-only --strict`; and inspect the scoped diff before checking this task.

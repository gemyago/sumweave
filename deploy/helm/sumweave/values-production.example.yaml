image:
  tag: git-commit-abcdef0
existingConfigMap: sumweave-production
existingSecret: sumweave-production
imagePullSecrets:
  - name: ghcr-registry
app:
  replicaCount: 2
  resources:
    requests: {cpu: 100m, memory: 256Mi}
worker:
  resources:
    requests: {cpu: 100m, memory: 256Mi}
scheduler:
  resources:
    requests: {cpu: 50m, memory: 128Mi}
migration:
  resources:
    requests: {cpu: 50m, memory: 128Mi}
httpRoute:
  enabled: true
  gatewayName: public
  namespace: gateway-system
  sectionName: https
  hostnames: [sumweave.example.com]
  rules:
    - matches:
        - path: {type: PathPrefix, value: /}
